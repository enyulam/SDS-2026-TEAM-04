import { TrainerStudentsScreen } from "@/features/trainer/trainer-students-screen";

/**
 * Screen `04` — Trainer Students (`P2-20`), at its canonical route
 * `/trainer/students` (inventory §7.2).
 */
export default function TrainerStudentsPage() {
  return <TrainerStudentsScreen />;
}
