# B.E.S.T Coach — Figma Design 2 Screen Implementation Matrix

**Status:** Planning artefact — created at the Step 7E0 governance reconciliation checkpoint (2026-07-30)
**Authority:** Governed by **Amendment 002 A-022** (Figma Design 2 is the final UI authority), **A-014** (three-flow, one-centre boundary), **A-016** (canonical hierarchy), **A-017** (mandatory nine dimensions), **A-018** (attendance), **A-019** (management administration), **A-020** (identity/invitation), **A-021** (one canonical report), **A-024** (revised phasing).
**Precedence:** This document is **procedural planning material**. It cannot override Specification v3, Amendment 001, Amendment 002, or `CLAUDE.md`.

---

## How to read this matrix

- **Figma Design 2 is authoritative for** visual layout, visual hierarchy, component composition, visible fields, screen labels, microcopy, page relationships, screen-to-screen interaction intent, visual states, and responsive behaviour **where explicitly shown**.
- **Figma Design 2 is NOT authoritative for** database schema, foreign-key design, RLS, server authorization, report lifecycle, audit behaviour, Auth implementation, AI governance, persistence architecture, state-machine rules, or transaction boundaries.
- **Routes are proposed, not ratified.** Every value in "Intended Next.js route" is a planning proposal and must be confirmed at its implementation checkpoint. No route has been created.
- **"Required data" describes conceptual data needs only.** It is **not** a schema, and it must not be read as one. Schema is decided by ratified governance plus the Step 7E preflight, never inferred from a visual frame.
- **No Figma asset has been scraped, exported, downloaded, or ported.** No Figma-generated React or CSS exists anywhere in this repository.

### Verified Figma reference

The only verified reference at this checkpoint is the **prototype entry link supplied by the orchestrator**:

`https://www.figma.com/proto/sSY1TYw3jyVlZDy8V2Mu7g/SDS-dashboard?node-id=391-2&t=lRaOIpHfwA4ZaNMZ-1`

> **This prototype-section link does not prove any individual child frame.** It identifies a prototype entry point only. No child frame, screen name, node ID, field, state, or asset is established by it. Every per-screen node ID below is therefore recorded as:
>
> **`Pending node-specific Design 2 link — do not guess`**
>
> Fabricating a node ID is prohibited (A-022).

### Status vocabulary

| Value | Meaning |
|---|---|
| `Planned — awaiting Design 2 verification` | The screen family is in ratified scope; its node-specific frame, states and assets are not yet verified. |
| `Conditional — scope decision unresolved` | Inclusion depends on an unresolved orchestrator decision. |

**No row is marked implemented, designed, verified, or ready.** Nothing in this matrix has been built.

---

## 1. Management flow

| Flow/role | Screen name | Functional purpose | Figma node ID/link | Intended Next.js route | Required data | Read actions | Write actions | Authorization boundary | Required loading state | Required empty state | Required validation state | Required error state | Required success state | Required disabled state | Reusable components | Asset/token requirements | Responsive requirements | Implementation phase/checkpoint | Verification/UAT requirement | Status | Notes/open discrepancy |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Management | Management login | Authenticate the single named management account | Pending node-specific Design 2 link — do not guess | `/login` (proposed, shared shell) | Supabase Auth identity only | Auth session probe | Sign-in attempt | Unauthenticated; Auth only — no domain data readable pre-auth | Yes — credential submission in flight | N/A | Yes — email/password format, required fields | Yes — invalid credentials, locked/unverified account, network failure | Yes — redirect to management dashboard | Yes — submit disabled while in flight | Auth shell, form field, primary button, inline alert | Logo, typography, colour, spacing, radii tokens | Desktop + mobile | Phase 3 of the A-024 sequence (identity/profile/invitation foundations) | Management can sign in; an unactivated profile cannot | Planned — awaiting Design 2 verification | Shared login shell vs three role-specific login screens is **undetermined** in Design 2 |
| Management | Management dashboard | Centre-scoped operational landing view | Pending node-specific Design 2 link — do not guess | `/management` (proposed) | Sole centre; class modules; upcoming class sessions; report-completion counts | Centre-scoped summary read | None | Management ↔ sole centre relationship, RLS-enforced | Yes | Yes — no class modules / no sessions yet | N/A | Yes — data load failure | N/A | KPI card, section header, empty-state panel | Icon set, typography, colour, spacing tokens | Desktop + mobile | Phase 4 (management setup and creation flows) | Dashboard shows only the sole centre's data | Planned — awaiting Design 2 verification | Exact KPI set on the Design 2 dashboard is unverified |
| Management | Management calendar | Date-based view of stored class sessions | Pending node-specific Design 2 link — do not guess | `/management/calendar` (proposed) | Class sessions (date, time, module, grade, assigned trainer) | Calendar projection read | None | Centre-scoped RLS | Yes | Yes — month with no sessions | N/A | Yes — load failure | N/A | Calendar grid, day cell, session list item, modal | Icon set, colour, typography, spacing tokens | Desktop + mobile | Phase 5 (trainer assignment and calendar projection) | Management-created sessions appear here; **no duplicated calendar-event record exists** | Planned — awaiting Design 2 verification | Calendar is a **projection** of class-session records (A-016) — never a separate event table |
| Management | Management overview / statistics | Approved-data operational monitoring for a class module | Pending node-specific Design 2 link — do not guess | `/management/classes/[moduleId]` (proposed) | Approved report completion; attendance; approved rating aggregates | Approved-data projection read | None | Centre-scoped RLS; **approved data only** | Yes | Yes — no approved data yet | N/A | Yes — load failure | N/A | KPI card, table, status pill, chart container | Chart/colour tokens, typography, spacing | Desktop + mobile | Phase 12 (management report view + remaining Design 2 management screens) | No path from any management view to a draft, internal note, or raw private rating | Planned — awaiting Design 2 verification | Whether Design 2 retains the v3 §8 "Class Health Summary" / "Management Insight" panels is **unverified**; if retained, `CLAUDE.md` §6's exact deterministic templates govern their content |
| Management | Create Class (entry) | Entry point to create a Class Module | Pending node-specific Design 2 link — do not guess | `/management/classes/new` (proposed) | Class Grade vocabulary (Beginner/Intermediate/Advanced); sole centre | Grade list read | None (entry step) | Management ↔ sole centre | Yes | N/A | N/A | Yes | N/A | Yes — until a grade is chosen | Form shell, stepper/section header, primary button | Typography, colour, spacing, radii | Desktop + mobile | Phase 2 (centre/grade/module/session foundations) → Phase 4 UI | UI labelled "Create Class"; persisted entity is a **Class Module** under a selected Class Grade | Planned — awaiting Design 2 verification | Whether Design 2 presents this as one form or a multi-step flow is **unverified** |
| Management | Class Grade selection | Choose Beginner / Intermediate / Advanced | Pending node-specific Design 2 link — do not guess | `/management/classes/new` (proposed, step 1) | The three Class Grade values only | Grade list read | None | Management ↔ sole centre | N/A | N/A | Yes — a grade must be selected | Yes | N/A | Yes — continue disabled until selected | Selector/chip group, radio group | Typography, colour, radii tokens | Desktop + mobile | Phase 2 → Phase 4 UI | Exactly three grade values exist; no fourth value is creatable | Planned — awaiting Design 2 verification | Whether Class Grade is a schema enum or a reference table is **unresolved (blocks Step 7E)** |
| Management | Class Module details | Capture the module's required details | Pending node-specific Design 2 link — do not guess | `/management/classes/new` (proposed, step 2) | Module fields per the approved final form; selected Class Grade | Draft read-back | **Create Class Module** (server-side, centre-scoped, validated, authorized, audited) | Management write guard + RLS | Yes | N/A | Yes — required fields, formats | Yes — write rejected/conflict | Yes — module created confirmation | Yes — submit disabled while invalid or in flight | Form field, select, textarea, primary/secondary button, inline alert | Typography, colour, spacing, radii | Desktop + mobile | Phase 2 → Phase 4 UI | Created module is centre-scoped and grade-linked | Planned — awaiting Design 2 verification | **Exact field inventory for Create Class is UNRESOLVED.** Blocks Step 7E **only if** a missing field changes the domain relationship model |
| Management | Class Session creation | Create one or more dated class sessions under a module | Pending node-specific Design 2 link — do not guess | `/management/classes/[moduleId]/sessions/new` (proposed) | Module; date; start time; end time (where the approved design supports them) | Module read | **Create Class Session(s)** (server-side, validated, audited) | Management write guard + RLS | Yes | N/A | Yes — date/time required, end after start | Yes — write rejected/conflict | Yes — session(s) created | Yes — submit disabled while invalid or in flight | Date picker, time picker, repeatable row, primary button | Typography, colour, spacing, radii | Desktop + mobile | Phase 2 → Phase 4 UI | Created sessions appear in the management calendar | Planned — awaiting Design 2 verification | Whether Design 2 supports batch/recurring session creation is **unverified** |
| Management | Trainer assignment | Assign exactly one trainer to a class session | Pending node-specific Design 2 link — do not guess | `/management/sessions/[sessionId]/assign` (proposed) | Class session; centre's trainer profiles | Trainer list read | **Assign trainer to class session** (server-side, audited) | Management write guard + RLS | Yes | Yes — no trainer profiles created yet | Yes — a trainer must be selected | Yes — assignment rejected | Yes — assignment confirmed | Yes — save disabled until a trainer is selected | Selector, list row, avatar/initial, primary button | Typography, colour, spacing tokens | Desktop + mobile | Phase 5 (trainer assignment and calendar projection) | The **same stored session** then appears in the assigned trainer's calendar — **no separate trainer-calendar copy** | Planned — awaiting Design 2 verification | Trainer assignment is authoritative at **class-session** level (A-016) |
| Management | Trainer creation / invitation | Create a trainer profile and send an account invitation | Pending node-specific Design 2 link — do not guess | `/management/trainers/new` (proposed) | Trainer profile fields; invitation email address | Existing trainer list read | **Create trainer profile**; **create invitation** (server-side, audited) | Management write guard + RLS | Yes | Yes — no trainers yet | Yes — required fields, email format, duplicate email | Yes — invitation send failure | Yes — profile created + invitation `pending` | Yes — submit disabled while invalid or in flight | Form field, email field, status pill, primary button | Typography, colour, spacing, radii | Desktop + mobile | Phase 3 (identity/profile/invitation foundations) | **No plaintext generated password** is stored, displayed, or emailed; profile is not an active login until activated | Planned — awaiting Design 2 verification | **Exact trainer-profile field inventory is UNRESOLVED**; invitation token/expiry implementation is **unresolved (Step 7E)** |
| Management | Student creation | Create a student profile | Pending node-specific Design 2 link — do not guess | `/management/students/new` (proposed) | Student profile fields | Existing student list read | **Create student profile** (server-side, audited) | Management write guard + RLS | Yes | Yes — no students yet | Yes — required fields, formats | Yes — write rejected | Yes — student created | Yes — submit disabled while invalid or in flight | Form field, primary button, inline alert | Typography, colour, spacing, radii | Desktop + mobile | Phase 3 → Phase 4 UI | Student profiles are centre-scoped; students have **no** login identity in this MVP | Planned — awaiting Design 2 verification | **Exact student-profile field inventory is UNRESOLVED**; students are profiles only — confirm no student Auth identity is implied by Design 2 |
| Management | Student enrolment | Enrol students into a Class Module | Pending node-specific Design 2 link — do not guess | `/management/classes/[moduleId]/enrolments` (proposed) | Class module; centre's student profiles; existing enrolments | Student + enrolment read | **Create/remove enrolment** (server-side, audited) | Management write guard + RLS | Yes | Yes — no students / none enrolled | Yes — at least one selection; duplicate enrolment | Yes — write rejected | Yes — enrolment saved | Yes — save disabled with no change | Multi-select list, checkbox row, primary button | Typography, colour, spacing tokens | Desktop + mobile | Phase 6 (student enrolment and roster projection) | Enrolled students appear in the correct session roster | Planned — awaiting Design 2 verification | Enrolment is at **Class Module** level; roster is the **session-level projection** of it (A-016) |
| Management | Parent creation / invitation | Create a parent profile and send an account invitation | Pending node-specific Design 2 link — do not guess | `/management/parents/new` (proposed) | Parent profile fields; invitation email address | Existing parent list read | **Create parent profile**; **create invitation** (server-side, audited) | Management write guard + RLS | Yes | Yes — no parents yet | Yes — required fields, email format, duplicate email | Yes — invitation send failure | Yes — profile created + invitation `pending` | Yes — submit disabled while invalid or in flight | Form field, email field, status pill, primary button | Typography, colour, spacing, radii | Desktop + mobile | Phase 3 (identity/profile/invitation foundations) | Invitation states include at least `pending`/`accepted`/`expired`/`revoked`; no plaintext password anywhere | Planned — awaiting Design 2 verification | **Exact parent-profile field inventory is UNRESOLVED** |
| Management | Parent–student linking | Link one or more student profiles to a parent | Pending node-specific Design 2 link — do not guess | `/management/parents/[parentId]/links` (proposed) | Parent profile; centre's student profiles; existing links | Parent + student + link read | **Create/revoke parent–student link** (server-side, audited) | Management write guard + RLS | Yes | Yes — no students / no links | Yes — at least one selection; duplicate link | Yes — write rejected | Yes — link saved | Yes — save disabled with no change | Multi-select list, chip, primary button | Typography, colour, spacing tokens | Desktop + mobile | Phase 3 → Phase 11 (parent view) | Parent authorization derives from the **live** link — parent A can never reach parent B's child | Planned — awaiting Design 2 verification | Whether Design 2 links from the parent screen, the student screen, or both is **unverified** |
| Management | Management report viewing | View a submitted report in the canonical format | Pending node-specific Design 2 link — do not guess | `/management/reports/[reportId]` (proposed) | **Submitted/approved report snapshot only** | Canonical submitted-report projection read | **None — view only** | Centre-scoped RLS + server-side **edit denial** | Yes | Yes — no submitted reports | N/A | Yes — load failure / not authorized | N/A | **Yes — no edit affordance; edit is server-denied regardless of UI** | **Shared canonical report presentation components** | Typography, colour, spacing, radii tokens | Desktop + mobile | Phase 10 (shared submitted-report projection) → Phase 12 | Management **cannot** edit; a direct edit call is rejected server-side; drafts, internal notes, raw private ratings and AI history are unreachable | Planned — awaiting Design 2 verification | **Exact report sections and field schema are UNRESOLVED** (U-06). One shared report read model across all three roles (A-021) |
| Management | Management shared states | Confirmation, validation, empty, loading, success and error states across management screens | Pending node-specific Design 2 link — do not guess | N/A — cross-cutting | N/A | N/A | N/A | Inherits each host screen's boundary | Yes | Yes | Yes | Yes | Yes | Yes | Modal/confirm dialog, toast, banner, skeleton, empty-state panel, inline field error | Icon set, colour, typography, spacing, radii, shadow tokens | Desktop + mobile | Applies to every management checkpoint | Each host screen demonstrates its own six states | Planned — awaiting Design 2 verification | Whether Design 2 supplies dedicated state frames or expects derivation from a base frame is **unverified** — do not derive silently, ask |

---

## 2. Trainer flow

| Flow/role | Screen name | Functional purpose | Figma node ID/link | Intended Next.js route | Required data | Read actions | Write actions | Authorization boundary | Required loading state | Required empty state | Required validation state | Required error state | Required success state | Required disabled state | Reusable components | Asset/token requirements | Responsive requirements | Implementation phase/checkpoint | Verification/UAT requirement | Status | Notes/open discrepancy |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Trainer | Trainer login | Authenticate an activated trainer identity | Pending node-specific Design 2 link — do not guess | `/login` (proposed, shared shell) | Supabase Auth identity only | Auth session probe | Sign-in attempt | Unauthenticated; Auth only | Yes | N/A | Yes — required fields, email format | Yes — invalid credentials, unactivated profile | Yes — redirect to trainer dashboard | Yes — submit disabled while in flight | Auth shell, form field, primary button, inline alert | Logo, typography, colour, spacing, radii | Desktop + mobile | Phase 3 (identity foundations) | A trainer profile that has not completed activation **cannot** sign in | Planned — awaiting Design 2 verification | Shared vs role-specific login screen is **undetermined** in Design 2 |
| Trainer | Trainer dashboard | Assigned-work landing view | Pending node-specific Design 2 link — do not guess | `/trainer` (proposed) | Sessions assigned to this trainer; pending report work | Assignment-scoped read | None | **Live class-session assignment**, RLS-enforced | Yes | Yes — no assigned sessions | N/A | Yes — load failure | N/A | KPI card, session list item, section header | Icon set, typography, colour, spacing | Desktop + mobile | Phase 5 (calendar projection) | Only assigned sessions are reachable | Planned — awaiting Design 2 verification | Exact dashboard composition unverified |
| Trainer | Trainer calendar | Date view of sessions assigned to this trainer | Pending node-specific Design 2 link — do not guess | `/trainer/calendar` (proposed) | The **same** class-session records, filtered by live assignment | Calendar projection read | None | Live class-session assignment, RLS-enforced | Yes | Yes — no assigned sessions in range | N/A | Yes — load failure | N/A | Calendar grid, day cell, session list item | Icon set, colour, typography, spacing | Desktop + mobile | Phase 5 (trainer assignment and calendar projection) | Management assignment causes the **same session** to appear here; **no duplicated calendar-event record**; unassigned sessions are unreachable | Planned — awaiting Design 2 verification | Calendars are projections, never copies (A-016, A-019) |
| Trainer | Assigned session details | Session context before starting work | Pending node-specific Design 2 link — do not guess | `/trainer/sessions/[sessionId]` (proposed) | Class session; module; grade; date/time; roster size | Assignment-scoped read | None | Live class-session assignment | Yes | Yes — no enrolled students | N/A | Yes — not assigned / load failure | N/A | Detail panel, metadata row, primary button | Typography, colour, spacing tokens | Desktop + mobile | Phase 5 → Phase 6 | A trainer cannot open a session they are not assigned to | Planned — awaiting Design 2 verification | Future-session lock behaviour (v3 §15) must be reconciled against the Design 2 frame |
| Trainer | Session roster | Live per-student workspace for the session | Pending node-specific Design 2 link — do not guess | `/trainer/sessions/[sessionId]/roster` (proposed) | Enrolled students; attendance state; previous focus; report status | Roster projection read | None (roster itself) | Live class-session assignment | Yes | Yes — no enrolled students | N/A | Yes — load failure | N/A | N/A | Student card, status pill, filter/sort control | Icon set, typography, colour, spacing, radii | Desktop + mobile | Phase 6 (student enrolment and roster projection) | Students enrolled in the module appear in this session's roster | Planned — awaiting Design 2 verification | Roster is the session-level projection of module enrolment |
| Trainer | Attendance | Confirm presence; toggle individuals to Absent | Pending node-specific Design 2 link — do not guess | `/trainer/sessions/[sessionId]/roster` (proposed, in-place) | Attendance per student per session | Attendance read | **Set attendance** (server-side, audited) | Live class-session assignment | Yes | Yes — no enrolled students | N/A | Yes — write rejected | Yes — attendance persisted | Yes — toggle disabled while saving | Toggle/switch, student row, toast | Icon set, colour, typography, spacing | Desktop + mobile | Phase 7 (trainer attendance) | **Default `Present`**; toggle to `Absent` persists for that student + session; uniqueness per student + session; **absent students receive no fabricated assessment or report**; changes are auditable | Planned — awaiting Design 2 verification | Whether Design 2 shows a distinct attendance screen or an in-roster control is **unverified** |
| Trainer | Nine-dimension assessment | Capture the full B.E.S.T observation for one student | Pending node-specific Design 2 link — do not guess | `/trainer/sessions/[sessionId]/students/[studentId]/assess` (proposed) | All 9 dimensions; 4 ratings; rubric anchors; strengths/focus; notes; follow-up | Existing observation read | **Save observation + all nine ratings** (server-side, validated, audited) | Live class-session assignment | Yes | N/A | **Yes — all nine dimensions required before completion** | Yes — save rejected / stale version | Yes — observation saved | Yes — generate disabled until all nine are rated | Rating chip group, rubric-anchor tooltip, notes field, banner | Typography, colour, spacing, radii, icon set | Desktop + mobile | Phase 8 (nine-dimension assessment) | **All nine ratings are required; no Quick mode exists; no four-dimension completion path exists** | Planned — awaiting Design 2 verification | If any Design 2 frame still shows a Quick/Full toggle or a 4-dimension form, that frame **conflicts with A-017** — governance wins; stop and report the discrepancy |
| Trainer | AI draft generation | Produce a governed AI draft from the saved observation | Pending node-specific Design 2 link — do not guess | `/trainer/sessions/[sessionId]/students/[studentId]/draft` (proposed) | Rubric-anchored skeleton from the authoritative saved observation | Observation re-read (authoritative) | **Create AI job / draft version** (server-side, idempotent, audited) | Live class-session assignment | **Yes — required** (synchronous generation) | N/A | Yes — blocked while the observation is incomplete | **Yes — required** (generation failure, assessment preserved, retry offered) | Yes — draft ready | Yes — actions disabled while generating | Loading/spinner state, banner, retry control | Icon set, colour, typography, spacing | Desktop + mobile | Phase 9 (AI draft, review, edit, approval) | Grounding validation can actually reject a draft; duplicate submission produces one version; **AI never publishes** | Planned — awaiting Design 2 verification | Design 2 governs the visual treatment of loading/failure/retry only — never the grounding or idempotency logic |
| Trainer | Report review | Review the draft against the trainer's own notes | Pending node-specific Design 2 link — do not guess | `/trainer/reports/[reportId]/review` (proposed) | Draft version; source map; trainer follow-up notes; quality checklist | Draft + source-map read | **Checklist item updates** (server-side, audited) | Live class-session assignment | Yes | N/A | Yes — checklist incomplete | Yes — load/save failure | Yes — checklist state saved | **Yes — approve disabled until all checklist items are true** | Checklist, compare panel, notes field, primary/secondary button | Typography, colour, spacing, radii | Desktop + mobile | Phase 9 | The disabled approve button is UX only — the **server independently re-verifies** the checklist | Planned — awaiting Design 2 verification | Exact Design 2 checklist wording is unverified; the governed checklist semantics are unchanged |
| Trainer | Report edit | Trainer-authorized editing of the governed editable version | Pending node-specific Design 2 link — do not guess | `/trainer/reports/[reportId]/edit` (proposed) | Governed editable report version | Editable version read | **Save edit** (new/updated editable version, server-side, audited) | Live class-session assignment; **trainer-only** | Yes | N/A | Yes — required content / stale version | Yes — save rejected / concurrency conflict | Yes — edit saved, returns to review | Yes — save disabled while unchanged or in flight | Rich text/textarea, primary/secondary button, banner | Typography, colour, spacing, radii | Desktop + mobile | Phase 9 | Editing **resets the quality checklist**; editing **never mutates a submitted approval snapshot in place**; edit requires review and approval again | Planned — awaiting Design 2 verification | Management/parent must be rejected **server-side** on this path, not merely denied the button (A-021) |
| Trainer | Report approval / submission | Approve and submit the final report | Pending node-specific Design 2 link — do not guess | `/trainer/reports/[reportId]/review` (proposed, confirm modal) | Report state; checklist columns; version | Report state read | **Guarded approve + publish transitions** (one server action, audit in the same transaction) | Live class-session assignment; **trainer-only** | Yes | N/A | Yes — checklist incomplete | Yes — guard rejects stale/invalid state | Yes — approved and submitted | Yes — disabled until checklist complete | Confirmation modal, primary/secondary button, toast | Icon set, typography, colour, spacing, radii, shadow | Desktop + mobile | Phase 9 | Approval snapshot is **immutable**; a direct API call with an incomplete checklist is rejected | Planned — awaiting Design 2 verification | The confirmation modal is a UX safeguard, never a security boundary |
| Trainer | Canonical report viewing (trainer) | View the submitted report in the canonical format | Pending node-specific Design 2 link — do not guess | `/trainer/reports/[reportId]` (proposed) | Submitted/approved report snapshot | **Shared canonical submitted-report projection read** | None on this view | Live class-session assignment | Yes | Yes — no submitted report yet | N/A | Yes — load failure | N/A | N/A on the view itself | **Shared canonical report presentation components** | Typography, colour, spacing, radii | Desktop + mobile | Phase 10 (shared submitted-report projection) | **One shared canonical report projection** is used by trainer, management and parent | Planned — awaiting Design 2 verification | **Exact report sections and field schema are UNRESOLVED** (U-06) |
| Trainer | Trainer shared states | Confirmation, validation, empty, loading, success and error states across trainer screens | Pending node-specific Design 2 link — do not guess | N/A — cross-cutting | N/A | N/A | N/A | Inherits each host screen's boundary | Yes | Yes | Yes | Yes | Yes | Yes | Modal/confirm dialog, toast, banner, skeleton, empty-state panel, inline field error | Icon set, colour, typography, spacing, radii, shadow tokens | Desktop + mobile | Applies to every trainer checkpoint | Each host screen demonstrates its own six states | Planned — awaiting Design 2 verification | The v3 §15 failure-and-recovery designs remain governing behaviour regardless of frame availability |

---

## 3. Parent flow

| Flow/role | Screen name | Functional purpose | Figma node ID/link | Intended Next.js route | Required data | Read actions | Write actions | Authorization boundary | Required loading state | Required empty state | Required validation state | Required error state | Required success state | Required disabled state | Reusable components | Asset/token requirements | Responsive requirements | Implementation phase/checkpoint | Verification/UAT requirement | Status | Notes/open discrepancy |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Parent | Invitation receipt / activation entry | Entry point reached from the invitation email | Pending node-specific Design 2 link — do not guess | `/invite/[token]` (proposed) | Invitation state only (`pending`/`accepted`/`expired`/`revoked`) | Invitation-state read | None (entry step) | Token-scoped; **no domain data readable** | Yes | N/A | Yes — malformed/absent token | Yes — expired, revoked, already accepted, unknown token | Yes — proceed to verification | Yes — continue disabled while validating | Auth shell, banner, primary button | Logo, typography, colour, spacing, radii | Desktop + mobile (email-first ⇒ mobile matters) | Phase 11 (parent invitation, activation, linked-student view) | Invitation states behave correctly; an unactivated profile is **not** a login identity | Planned — awaiting Design 2 verification | **Invitation token and expiry implementation details are UNRESOLVED (Step 7E)**; **no plaintext password may ever appear here** |
| Parent | Account verification | Verify ownership of the invited email address | Pending node-specific Design 2 link — do not guess | `/invite/[token]/verify` (proposed) | Invitation + Auth verification state | Verification-state read | **Verification confirmation** (Auth) | Token-scoped | Yes | N/A | Yes — code/link required or malformed | Yes — verification failed/expired | Yes — verified, proceed to credential setup | Yes — submit disabled while in flight | Auth shell, form field, banner, primary button | Typography, colour, spacing, radii | Desktop + mobile | Phase 11 | Recipient verifies the account before any credential is set | Planned — awaiting Design 2 verification | Whether Design 2 uses a magic link, a code, or both is **unverified** |
| Parent | Credential setup | Recipient establishes their own login credentials | Pending node-specific Design 2 link — do not guess | `/invite/[token]/set-password` (proposed) | Verified invitation | None beyond invitation state | **Set own credentials** (Auth); mark invitation `accepted` | Token-scoped, single-use | Yes | N/A | Yes — password policy, confirmation match | Yes — token expired/reused, policy rejection | Yes — account active, redirect to login or session | Yes — submit disabled while invalid or in flight | Auth shell, password field, strength/requirement list, primary button | Typography, colour, spacing, radii | Desktop + mobile | Phase 11 | **No plaintext generated password is stored, displayed, or emailed** — the recipient sets their own | Planned — awaiting Design 2 verification | Password policy source is **unverified**; Supabase Auth defaults vs a stricter policy must be confirmed |
| Parent | Parent login | Authenticate an activated parent identity | Pending node-specific Design 2 link — do not guess | `/login` (proposed, shared shell) | Supabase Auth identity only | Auth session probe | Sign-in attempt | Unauthenticated; Auth only | Yes | N/A | Yes — required fields, email format | Yes — invalid credentials, unactivated profile | Yes — redirect to linked-student view | Yes — submit disabled while in flight | Auth shell, form field, primary button, inline alert | Logo, typography, colour, spacing, radii | Desktop + mobile | Phase 11 | An unactivated parent profile **cannot** sign in | Planned — awaiting Design 2 verification | Shared vs role-specific login screen is **undetermined** in Design 2 |
| Parent | Linked-student view | Landing view of the parent's linked student(s) | Pending node-specific Design 2 link — do not guess | `/parent` (proposed) | **Only** students in live parent–student links | Link-scoped read | None | **Live parent–student relationship**, RLS-enforced | Yes | Yes — no linked students yet | N/A | Yes — load failure | N/A | N/A | Student card, section header, empty-state panel | Icon set, typography, colour, spacing, radii | Desktop + mobile | Phase 11 | Parent A can never see parent B's child — refused at the **database** level, not hidden in the UI | Planned — awaiting Design 2 verification | Multi-child layout in Design 2 is **unverified** (A parent may be linked to more than one student) |
| Parent | Canonical report list / report access | Reach a specific submitted report for a linked student | Pending node-specific Design 2 link — do not guess | `/parent/students/[studentId]` (proposed) | **Submitted/approved report snapshots only**, for linked students | Link-scoped submitted-report list read | None | Live parent–student relationship | Yes | Yes — no submitted reports yet ("being prepared" state) | N/A | Yes — load failure | N/A | N/A | List row, status pill, calendar or list container, empty-state panel | Icon set, typography, colour, spacing | Desktop + mobile | Phase 11 | An unapproved report is **never** listed or reachable; the pending state exposes no draft content | Planned — awaiting Design 2 verification | Whether Design 2 uses a calendar or a list for parent report access is **unverified** |
| Parent | Canonical report viewing (parent) | View the submitted report in the canonical format | Pending node-specific Design 2 link — do not guess | `/parent/reports/[reportId]` (proposed) | Submitted/approved report snapshot for a linked student | **Shared canonical submitted-report projection read** | **None — view only** | Live parent–student relationship + server-side **edit denial** | Yes | Yes — no report content | N/A | Yes — load failure / not authorized | N/A | **Yes — no edit affordance; edit is server-denied regardless of UI** | **Shared canonical report presentation components** | Typography, colour, spacing, radii | Desktop + mobile | Phase 10 → Phase 11 | Parent **cannot** edit (server-rejected); **no** drafts, internal notes, raw private ratings, or AI history; **no per-dimension rating grid** | Planned — awaiting Design 2 verification | **Exact report sections and field schema are UNRESOLVED** (U-06). Evidence media on this screen is **conditional and unresolved** — Amendment 001 A-001 gating applies in full if it is retained |
| Parent | Unauthorized / unlinked access state | Correct refusal surface for an unlinked or unauthorized request | Pending node-specific Design 2 link — do not guess | Cross-cutting (`not-found` / `forbidden` boundary, proposed) | None — nothing about the requested resource may leak | None | None | Refusal is decided by **RLS + server guard**, never by the page | N/A | N/A | N/A | **Yes — required** | N/A | N/A | Error/refusal panel, primary button back to safety | Icon set, typography, colour, spacing | Desktop + mobile | Phase 11 | Manipulating a URL/ID to another child's report is refused at the database level; the refusal surface leaks **no** existence or content detail | Planned — awaiting Design 2 verification | Whether Design 2 supplies a dedicated refusal frame is **unverified** — if absent, stop and request it rather than inventing one |
| Parent | Parent shared states | Loading, empty, success and error states across parent screens | Pending node-specific Design 2 link — do not guess | N/A — cross-cutting | N/A | N/A | N/A | Inherits each host screen's boundary | Yes | Yes | Yes (activation forms) | Yes | Yes | Yes | Toast, banner, skeleton, empty-state panel, inline field error | Icon set, colour, typography, spacing, radii, shadow tokens | Desktop + mobile | Applies to every parent checkpoint | Each host screen demonstrates its own states | Planned — awaiting Design 2 verification | The v3 §15 "Progress update being prepared" calm pending state remains governing behaviour |

---

## 4. Conditional / deferred screen families (not in the ratified three-flow completion scope)

| Flow/role | Screen name | Functional purpose | Figma node ID/link | Intended Next.js route | Required data | Read actions | Write actions | Authorization boundary | Required loading state | Required empty state | Required validation state | Required error state | Required success state | Required disabled state | Reusable components | Asset/token requirements | Responsive requirements | Implementation phase/checkpoint | Verification/UAT requirement | Status | Notes/open discrepancy |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Deferred | Teaching Assistant flow (all screens) | TA evidence workflow | Pending node-specific Design 2 link — do not guess | Not proposed | Not defined | — | — | — | — | — | — | — | — | — | — | — | — | **Deferred — not an MVP completion gate (A-014)** | **Not an MVP UAT persona (A-024)** | Conditional — scope decision unresolved | TA screens, TA login, and TA UAT are deferred. TA evidence-upload permissions are **not** transferred to management or trainer |
| Conditional | Evidence media surfaces | Capture/attach/view evidence media | Pending node-specific Design 2 link — do not guess | Not proposed | Not defined | — | — | **Amendment 001 A-001 gating applies in full if implemented** | — | — | — | — | — | — | — | — | — | Evidence implementation checkpoint only | Amendment 001 A-003 prohibited/permitted paths, A-004 both-direction Parent UAT | Conditional — scope decision unresolved | **UNRESOLVED:** whether evidence remains a completion requirement (U-09); **who uploads it** (U-10); the exact evidence Figma screens (U-11). No replacement uploader may be invented |

---

## 5. Orchestrator Figma Porting Actions

This is the **mandatory implementation-readiness checklist** required by Amendment 002 **A-022.1**. It must be satisfied for a screen family **before that family's UI implementation checkpoint begins**.

**Blocking rule (A-022.3):** a missing item below blocks **only** the affected UI implementation checkpoint. It does **not** block Step 7E **unless** a missing visible field changes the actual domain relationship model. Nothing here may be guessed or silently recreated — implementation must **stop and ask**.

### 5.1 Cross-cutting actions (apply to every screen family)

| Owner | Affected screen family | Blocking checkpoint | Required action | Status | Notes |
|---|---|---|---|---|---|
| Orchestrator | All (Management, Trainer, Parent) | First Figma-based UI implementation checkpoint | **Obtain** a node-specific Figma `/design/` link for each approved screen and each of its state variants; **confirm** the authoritative screen name and its user flow | Outstanding | The supplied `/proto/` entry link does not establish any child frame |
| Orchestrator | All | First Figma-based UI implementation checkpoint | **Export/obtain** the design-variable set, or an approved **token inventory** if variables are not defined | Outstanding | Tokens govern the MVP design system; ad-hoc values from generated CSS are prohibited |
| Orchestrator | All | First Figma-based UI implementation checkpoint | **Confirm** fonts and full **typography** scale (family, weight, size, line-height, letter-spacing) and their licensing for web use | Outstanding | Font licensing must be confirmed before any font file enters the repository |
| Orchestrator | All | First Figma-based UI implementation checkpoint | **Confirm** the **colour** palette including semantic roles (surface, text, border, success, warning, error, disabled) and contrast against WCAG 2.2 AA | Outstanding | Contrast is checked against the standard, never eyeballed |
| Orchestrator | All | First Figma-based UI implementation checkpoint | **Confirm** the **spacing** scale | Outstanding | — |
| Orchestrator | All | First Figma-based UI implementation checkpoint | **Confirm** the **radii** scale | Outstanding | — |
| Orchestrator | All | First Figma-based UI implementation checkpoint | **Confirm** the **shadow/elevation** scale | Outstanding | — |
| Orchestrator | All | First Figma-based UI implementation checkpoint | **Export** the **original logo** assets (full lockup and mark, light and dark variants) at source fidelity | Outstanding | Original vector, not a screenshot or a rasterised prototype capture |
| Orchestrator | All | First Figma-based UI implementation checkpoint | **Export** the approved **SVG icon** set with names | Outstanding | Icon set must be complete for the screens in scope |
| Orchestrator | All | First Figma-based UI implementation checkpoint | **Export** approved **image assets** with intended usage and alt-text intent | Outstanding | Any image resembling a real child is prohibited (synthetic only, ADR-6) |
| Orchestrator | All | Each affected UI checkpoint | **Obtain** the **responsive variants** (desktop / tablet / mobile) actually designed, and **confirm** which breakpoints are authoritative | Outstanding | Responsive behaviour is authoritative only where explicitly shown |
| Orchestrator | All | Each affected UI checkpoint | **Confirm** **prototype transitions** and any navigation intent not evident from a static frame | Outstanding | Prototype-only navigation must never be ported as application routing |
| Orchestrator | All | Each affected UI checkpoint | **Confirm** **interaction notes** — hover, focus, keyboard, and gesture behaviour not visible in a static frame | Outstanding | Full keyboard operability is required (WCAG 2.2 AA) |
| Orchestrator | All | Each affected UI checkpoint | **Verify** the six required state frames per screen: **loading, empty, validation, error, success, disabled** | Outstanding | Where a state frame is absent, stop and request it — do not derive it silently |
| Orchestrator | All | Each affected UI checkpoint | **Reconcile** every discrepancy between a Figma frame and a ratified governance or domain rule, and record the resolution | Outstanding | Governance wins over a frame; the discrepancy is still recorded, never silently dropped |
| Orchestrator + Claude Code | All | Each affected UI checkpoint | **Confirm** that **no generated React, no generated CSS, no Figma mock data, no prototype business logic, no fake authentication, and no client-side authorization assumption** is being copied blindly | Outstanding | Presentation and approved assets only (A-022.2) |
| Orchestrator | All | Each affected UI checkpoint | **Confirm** no ported artefact introduces a **duplicated calendar record**, a **hard-coded user identity**, or a **shortcut bypassing Supabase, RLS, or server-side validation** | Outstanding | Calendars are projections (A-016); identities come from Auth + profiles (A-020) |

### 5.2 Per-screen-family actions

| Owner | Affected screen family | Blocking checkpoint | Required action | Status | Notes |
|---|---|---|---|---|---|
| Orchestrator | Management login | Identity/auth UI checkpoint | **Confirm** whether Design 2 has one shared login screen or three role-specific ones; **obtain** the node link and its error/validation states | Outstanding | Route shape depends on this answer |
| Orchestrator | Management dashboard | Management UI checkpoint | **Obtain** the node link; **confirm** the exact KPI/section set; **verify** empty and loading frames | Outstanding | KPI set must be derivable from approved data only |
| Orchestrator | Management calendar | Calendar projection checkpoint | **Obtain** the node link; **confirm** month/week/day views, session cell content, and the drill-down target; **reconcile** that the calendar is a projection, not a stored event list | Outstanding | No duplicated calendar-event record is permitted |
| Orchestrator | Management overview / statistics | Management report/statistics checkpoint | **Obtain** the node link(s); **confirm** which panels survive in Design 2; **reconcile** against `CLAUDE.md` §6's exact deterministic templates if the health-summary/insight panels are retained | Outstanding | These panels must never become AI-authored prose |
| Orchestrator | Create Class (entry) | Class-module creation checkpoint | **Obtain** the node link; **confirm** whether it is a single form or a multi-step flow | Outstanding | — |
| Orchestrator | Class Grade selection | Class-module creation checkpoint | **Obtain** the node link; **confirm** the control type and that exactly three values appear | Outstanding | Grade vocabulary is closed: Beginner / Intermediate / Advanced |
| Orchestrator | Class Module details | **Step 7E if relationship-changing**, otherwise the class-module creation checkpoint | **Obtain** the node link; **confirm the exact field inventory**; **reconcile** any field implying a relationship the ratified hierarchy does not have | **Outstanding — schema-relevant** | This is the one Create Class item that can escalate to a Step 7E blocker |
| Orchestrator | Class Session creation | Session-scheduling checkpoint | **Obtain** the node link; **confirm** date/start/end support and whether batch or recurring creation is designed | Outstanding | — |
| Orchestrator | Trainer assignment | Assignment/calendar checkpoint | **Obtain** the node link; **confirm** single-trainer-per-session in the design; **verify** the empty state when no trainers exist | Outstanding | Assignment is authoritative at class-session level |
| Orchestrator | Trainer creation / invitation | **Step 7E if relationship-changing**, otherwise the identity checkpoint | **Obtain** the node link; **confirm the exact trainer-profile field inventory**; **confirm** no password is ever displayed | **Outstanding — schema-relevant** | Invitation token/expiry design is a separate Step 7E item |
| Orchestrator | Student creation | **Step 7E if relationship-changing**, otherwise the student checkpoint | **Obtain** the node link; **confirm the exact student-profile field inventory**; **confirm** no student login identity is implied | **Outstanding — schema-relevant** | — |
| Orchestrator | Student enrolment | Enrolment/roster checkpoint | **Obtain** the node link; **confirm** enrolment is at Class Module level in the design; **verify** empty and duplicate-selection states | Outstanding | — |
| Orchestrator | Parent creation / invitation | **Step 7E if relationship-changing**, otherwise the identity checkpoint | **Obtain** the node link; **confirm the exact parent-profile field inventory**; **confirm** no password is ever displayed | **Outstanding — schema-relevant** | — |
| Orchestrator | Parent–student linking | Linking checkpoint | **Obtain** the node link; **confirm** whether linking is initiated from the parent screen, the student screen, or both; **confirm** multi-student linking support | Outstanding | Parent authorization derives from these live links |
| Orchestrator | Management report viewing | Report projection checkpoint | **Obtain** the canonical report frame; **confirm** it is the same frame used for trainer and parent; **reconcile** that no edit affordance exists for management | Outstanding | One canonical format across all three roles (A-021) |
| Orchestrator | Management shared states | Every management checkpoint | **Verify** whether dedicated state frames exist; **obtain** them or **confirm** the derivation rule explicitly | Outstanding | Silent derivation is prohibited |
| Orchestrator | Trainer login | Identity/auth UI checkpoint | **Confirm** shared vs role-specific login; **obtain** the node link and states | Outstanding | — |
| Orchestrator | Trainer dashboard | Trainer UI checkpoint | **Obtain** the node link; **confirm** the assigned-work composition; **verify** the empty state | Outstanding | — |
| Orchestrator | Trainer calendar | Calendar projection checkpoint | **Obtain** the node link; **reconcile** that it renders the same stored session records management created | Outstanding | No trainer-calendar copy |
| Orchestrator | Assigned session details | Trainer session checkpoint | **Obtain** the node link; **confirm** the future-session lock treatment | Outstanding | v3 §15 lock behaviour must be represented or explicitly deferred |
| Orchestrator | Session roster | Roster checkpoint | **Obtain** the node link; **confirm** card content, previous-focus surface, filters and sort | Outstanding | — |
| Orchestrator | Attendance | Attendance checkpoint | **Obtain** the node link; **confirm** whether attendance is a distinct screen or an in-roster control; **verify** the Present-default and Absent-toggle visual states | Outstanding | Default `Present`, toggle to `Absent` (A-018) |
| Orchestrator | Nine-dimension assessment | Assessment checkpoint | **Obtain** the node link; **confirm** all nine dimensions and four ratings appear; **reconcile** any Quick/Full toggle or 4-dimension frame as a **governance conflict** | **Outstanding — governance-conflict risk** | A-017 wins over any frame that still shows Quick mode |
| Orchestrator | AI draft generation | AI drafting checkpoint | **Obtain** the node link; **confirm** loading, failure and retry treatments | Outstanding | Visual treatment only — grounding/idempotency logic is never inferred from a frame |
| Orchestrator | Report review | Review checkpoint | **Obtain** the node link; **confirm** the exact checklist items and the compare-with-notes surface | Outstanding | Server-side checklist re-verification is unchanged |
| Orchestrator | Report edit | Edit checkpoint | **Obtain** the node link; **confirm** the editable surface; **reconcile** that edit is trainer-only and server-enforced | Outstanding | Edit-button visibility is never authorization |
| Orchestrator | Report approval / submission | Approval checkpoint | **Obtain** the node link and the confirmation-modal frame; **confirm** the disabled-until-checklist treatment | Outstanding | The modal is a UX safeguard, not a gate |
| Orchestrator | Canonical report viewing (trainer) | Report projection checkpoint | **Obtain** the canonical report frame; **confirm** it is the shared one | Outstanding | Same frame as management and parent |
| Orchestrator | Trainer shared states | Every trainer checkpoint | **Verify** dedicated state frames; **obtain** or **confirm** the derivation rule | Outstanding | — |
| Orchestrator | Invitation receipt / activation entry | Parent activation checkpoint | **Obtain** the node link; **confirm** expired/revoked/already-accepted treatments | Outstanding | No plaintext password may appear |
| Orchestrator | Account verification | Parent activation checkpoint | **Obtain** the node link; **confirm** magic link vs code vs both | Outstanding | — |
| Orchestrator | Credential setup | Parent activation checkpoint | **Obtain** the node link; **confirm** the password-policy copy and requirement list | Outstanding | Recipient sets their own credentials (A-020) |
| Orchestrator | Parent login | Identity/auth UI checkpoint | **Confirm** shared vs role-specific login; **obtain** the node link and states | Outstanding | — |
| Orchestrator | Linked-student view | Parent view checkpoint | **Obtain** the node link; **confirm** the multi-child layout; **verify** the empty state | Outstanding | — |
| Orchestrator | Canonical report list / report access | Parent view checkpoint | **Obtain** the node link; **confirm** calendar vs list; **verify** the "being prepared" pending state | Outstanding | Pending state must expose no draft content |
| Orchestrator | Canonical report viewing (parent) | Report projection checkpoint | **Obtain** the canonical report frame; **confirm the exact report sections and field schema**; **reconcile** that no per-dimension rating grid appears | **Outstanding — highest priority** | U-06. Any rating grid in a parent frame is a **confirmed leak** and must be removed |
| Orchestrator | Unauthorized / unlinked access state | Parent view checkpoint | **Obtain** the node link, or **confirm** that no refusal frame exists so one can be specified rather than invented | Outstanding | The refusal surface must leak no existence or content detail |
| Orchestrator | Parent shared states | Every parent checkpoint | **Verify** dedicated state frames; **obtain** or **confirm** the derivation rule | Outstanding | — |
| Orchestrator | Teaching Assistant flow | Deferred — no MVP checkpoint | **Confirm** the TA flow stays deferred, or reopen it as an explicit scope change | Outstanding | Not an MVP completion gate (A-014, A-024) |
| Orchestrator | Evidence media surfaces | Evidence implementation checkpoint only | **Confirm** whether evidence remains in scope; **confirm who uploads it**; **obtain** the exact evidence Figma screens if retained | **Outstanding — scope decision** | U-09, U-10, U-11. No replacement uploader may be invented; Amendment 001 A-001/A-003/A-004 apply in full if retained |

---

*Created 2026-07-30 at the Step 7E0 governance reconciliation checkpoint. No Figma asset was scraped, exported, downloaded, or ported to produce this document, and no node ID was fabricated.*
