# 48-Hour Frontend Workstream — Progress Log

## Header

| Field | Value |
|---|---|
| **Workstream** | 48-hour physical-test slice — **frontend** |
| **Owning agent** | **Codex** |
| **Owning branch** | `feat/48h-frontend` |
| **Planned worktree path** | `C:\Users\enyul\Vibe Studio\B.E.S.T-Coach-Workspace\worktrees\frontend-48h` |
| **Contract path** | `docs/plan/PHYSICAL_TEST_SLICE_48H.md` |
| **Contract baseline commit** | The commit created by `docs(plan): define 48-hour physical-test slice` — the **merge-base** of `feat/48h-backend` and `feat/48h-frontend`. Resolve at any time with `git merge-base feat/48h-backend feat/48h-frontend`; equivalently, the tip of `main` at worktree creation. Both branches start from this exact commit. |
| **Created** | 2026-08-05 (Asia/Singapore) |

> **This is an operational log, not a governance authority.**
>
> **Governance and the shared contract take precedence over anything recorded here.** Precedence is unchanged: Specification v3 → Amendments 001–004 → `CLAUDE.md` → the Implementation Plan → Figma Design 2 → `STATUS.md` → `BUILD_NOTES.md` → the migration tracker → `docs/plan/PHYSICAL_TEST_SLICE_48H.md` → **this log**. Nothing written in this file amends, relaxes or reinterprets any of them. Where this log and a governing document disagree, the governing document wins and the disagreement is raised as a blocker under contract §9.

**Ownership.** This log is **branch-local and owned by the frontend workstream only**. `docs/workstreams/48H_BACKEND_PROGRESS.md` is **read-only** here. Shared references (`CLAUDE.md`, `docs/spec/**`, `docs/plan/**`, `docs/progress/**`) are **read-only during parallel work**. `STATUS.md`, `BUILD_NOTES.md`, both migration trackers, Amendment 004, the Step 7I baseline, the Figma matrix and the 48-hour contract are **never** updated from this worktree — canonical progress is reconciled once from `main`.

**Commit discipline.** Each agent updates and commits its own workstream log **in the same commit as the corresponding implementation checkpoint**, unless the checkpoint is blocked before any implementation change.

**Prohibited content.** No secrets, passwords, tokens, `.env` values or personal data in this log — ever, including in a quoted error, a command transcript or a stack trace.

---

## Fixed status vocabulary

Use exactly these six values. Do not invent a seventh, and do not qualify one with an adverb.

- **Not started**
- **In progress**
- **Blocked**
- **Ready for review**
- **Accepted**
- **Integrated**

---

## Round checklist

| # | Round / item | Status |
|---|---|---|
| F1.1 | **Round F1** — design foundations and shell | Ready for review |
| F1.2 | **Round F1** — login and role presentation | Ready for review |
| F1.3 | **Round F1** — Trainer dashboard and roster | Ready for review |
| F1.4 | **Round F1** — assessment | Ready for review |
| F1.5 | **Round F1** — AI loading/failure/retry states | Ready for review |
| F1.6 | **Round F1** — trainer review/edit/checklist/approval | Ready for review |
| F2.1 | **Round F2** — Management queue and review | Ready for review |
| F2.2 | **Round F2** — wording editor and return dialog | Ready for review |
| F2.3 | **Round F2** — correction and reapproval UI | Ready for review |
| F2.4 | **Round F2** — Parent list and report detail | Ready for review |
| F2.5 | **Round F2** — privacy and absence tests | Ready for review |
| F-01b | **Bounded correction** — Tailwind utility precedence on form controls (WCAG 2.2 AA) | Ready for review |
| F3.1 | Real-adapter wiring support | Not started |
| F3.2 | Physical-test blocker fixes | Not started |

---

## Owned paths (contract §7.2)

`app/(auth)/**` · `app/(portals)/**` · `components/**` · `features/**` · `lib/frontend/**` · `tests/frontend/**` · `public/brand/**` · `app/layout.tsx` · `app/globals.css` · `docs/workstreams/48H_FRONTEND_PROGRESS.md`

**Must not edit:** SQL or migrations · RLS or grants · generated database types · server authorization · lifecycle rules · governance documents · `package.json` · `package-lock.json` · the backend workstream log.

Any genuinely required cross-owned edit is a **blocker**, reported **before** modification.

---

## Standing frontend constraints (contract §5, §7.3)

- **Frontend contracts must not import generated database types directly** — `lib/frontend/**` consumes only the DTO inventory of contract §5.3.
- **The `role` query parameter selects presentation only.** It carries no authority. Authority is server-derived, always.
- **`ManagementQueueRowDto` / `ManagementReviewDto` exclusions are absolute** — no ratings, observations, attendance, evidence, trainer notes, checklist values, content hashes, revision counts or AI history; correction reasons only where contract §5.5 permits.
- **No per-dimension rating grid on any parent surface**, in any form or wording.
- **`unauthorized` and `unavailable` must be non-disclosing** and byte-indistinguishable from "no such report".
- **Eight Figma design families are `Blocked — new design required`** — management review queue · management final review · wording-only editor · return-to-trainer dialog · correction tracking · final Approve & Submit · staff notification surface · parent notification surface. **Do not invent a frame, node ID or field for any of them.** Build to the contract's field lists; report any point where that is insufficient.
- **No visual asset enters the repository** without a recorded `PORT` / `REFERENCE ONLY` / `REBUILD` / `REJECT` / `NOT APPLICABLE` disposition.
- **All nine dimensions are mandatory.** No Quick mode, no four-dimension completion path, no `mode` toggle in any component prop, validator or test.
- **WCAG 2.2 AA** — semantic HTML, landmarks, real labels, sane focus order, full keyboard operability, contrast checked rather than eyeballed.
- **Fixture mode is a development switch only** and must never be reachable from a participant-facing surface (gate G-19).

---

## Append-only checkpoint template

**Append entries below. Never rewrite or delete an existing entry.** Every checkpoint entry must contain all eleven fields.

```markdown
### <YYYY-MM-DD HH:MM Asia/Singapore> — <Round / checkpoint ID>

- **Timestamp (Asia/Singapore):**
- **Round / checkpoint ID:**
- **Starting commit:**
- **Ending commit:**
- **Status:**              <Not started | In progress | Blocked | Ready for review | Accepted | Integrated>
- **Scope completed:**
- **Files changed:**
- **Tests and validation:** <command, exit code, result — no credential-bearing output>
- **Unresolved blockers:**
- **Contract deviations requested:** <none, or the exact decision required>
- **Next action:**
```

---

## Checkpoint entries

### 2026-08-05 18:10 Asia/Singapore — Round F1

- **Timestamp (Asia/Singapore):** 2026-08-05 18:10
- **Round / checkpoint ID:** Round F1
- **Starting commit:** `68169e97cbf614bf8b9b55deaee4039065fa45a0`
- **Ending commit:** The commit created by `feat(trainer): deliver fixture-backed F1 slice`
- **Status:** Ready for review
- **Scope completed:** Shared dark-navy foundations and responsive Trainer portal shell; role-tab login presentation; Trainer dashboard, session roster and synthetic empty roster; complete nine-dimension assessment with the governed four-level scale and all-nine save gate; deterministic observation save failure/recovery; deterministic AI generation loading, safe first failure, bounded retry and success; four-panel report review, Trainer wording editor, exact three-item checklist, non-publishing Trainer approval, returned-report queue/correction banner, and loading, empty, unavailable, validation and disabled states. The `PhysicalTestPort` and its deterministic browser-session fixture adapter use the contract DTO/action names and visibly identify fixture mode as ineligible for participant use.
- **Files changed:** `app/layout.tsx`; `app/globals.css`; `app/(auth)/**`; `app/(portals)/trainer/**`; `components/**`; `features/**`; `lib/frontend/**`; `tests/frontend/**`; this frontend workstream log.
- **Tests and validation:** `npm ci` — exit 0, exact locked dependencies installed with no manifest changes; `node_modules/.bin/tsc.cmd --noEmit` — exit 0, including typed fixture/action assertions; `node_modules/.bin/eslint.cmd .` — exit 0; `npm.cmd run build` — exit 0, production build completed for all F1 routes; `node tests/frontend/trainer-browser-smoke.mjs` against the production build — exit 0, full Trainer walkthrough passed with zero uncaught browser-console/runtime errors. No frontend test script or test-runner dependency exists, so no package test command was available and none was added.
- **Unresolved blockers:** No blocker to independent Round F1 review. A real participant-test adapter remains dependent on backend authentication, server-derived session authority, contract action endpoints, persistence/projections, lifecycle enforcement and the grounded AI service. Returned-report correction authoring/reaffirmation and all Management/Parent surfaces remain deliberately deferred beyond F1.
- **Contract deviations requested:** none
- **Next action:** Independent F1 review and acceptance; keep the fixture adapter out of participant surfaces, then reconcile the backend contract before Round F2/F3 adapter work.

### 2026-08-05 19:06 Asia/Singapore — Round F2

- **Timestamp (Asia/Singapore):** 2026-08-05 19:06
- **Round / checkpoint ID:** Round F2
- **Starting commit:** `76540834858bd4cc0b40dc054a408a3e942b6251`
- **Ending commit:** The commit created by `feat(frontend): complete fixture-backed F2 lifecycle`
- **Status:** Ready for review
- **Scope completed:** Independent F1 verification passed without repair. Extended the typed `PhysicalTestPort`, deterministic fixture adapter and browser-session state through Management pending review, wording-only edit, bounded return, Trainer correction or explicit reaffirmation, fresh checklist and reapproval, Management final submission, and Parent canonical visibility. Added `/management`, Management queues/review/editor, `/parent`, Parent availability/list/canonical detail, durable queue/badge/banner feedback, privacy-safe loading/empty/linked-unavailable/unavailable/denied states, runtime fixture lifecycle assertions, DOM privacy checks and a complete three-role browser walkthrough. Fixture mode remains visibly ineligible for participant testing; no real adapter, Supabase access, authentication or server lifecycle implementation was added.
- **Files changed:** `app/(portals)/management/**`; `app/(portals)/parent/**`; `components/layout/portal-shell.tsx`; `components/ui/state-panel.tsx`; `features/auth/login-presentation.tsx`; `features/management/**`; `features/parent/**`; bounded Trainer correction/reapproval changes under `features/trainer/**`; `lib/frontend/contracts/physical-test.ts`; `lib/frontend/fixtures/physical-test-fixture.ts`; `lib/frontend/physical-test-port.ts`; `tests/frontend/**`; this frontend workstream log.
- **Tests and validation:** baseline/ownership checks — pass; independent F1 `tsc --noEmit`, ESLint, production build and complete Trainer browser smoke — exit 0; F2 `tsc --noEmit` — exit 0; `eslint .` — exit 0; `npm.cmd run build` — exit 0 for all Trainer, Management and Parent routes; compiled runtime `fixture-lifecycle.assertions.ts` — exit 0, including stale duplicate actions and canonical visibility; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0 with Management/Parent DOM absence checks and zero uncaught browser-console/runtime errors; `git diff --check` — exit 0.
- **Unresolved blockers:** No blocker to fixture-backed F2 review. Participant testing still depends on backend Round B2 and integration: real authentication and server-derived role/relationship authority; governed observation read/write implementation; server actions for the exact port methods; queue/list projections (contract CP-3 remains open); AI provider wiring; durable server-backed in-app projections; and final real-adapter wiring after backend-first integration. External notification delivery and post-submission correction initiation remain intentionally deferred.
- **Contract deviations requested:** none
- **Next action:** Independent F2/integration review, backend-first integration per the pinned order, then wire the real participant adapter without changing page components.

### 2026-08-06 — FRONTEND RECONSTRUCTION F1 (Shared Visual Foundation)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F1` — the shared visual foundation of the screen-by-screen reconstruction plan. **This is not the historical `Round F1` above**; that entry records the earlier fixture-backed Trainer delivery round. Reconstruction checkpoints are always written in full.
- **Starting commit:** `b60b44d31f55ac9c1c03301511b63748ed1399d7`
- **Ending commit:** The commit created by `feat(frontend): establish final MVP visual foundation`
- **Status:** Ready for review
- **Scope completed:** Established the shared final-MVP visual foundation derived from the twelve frozen `reference.png` files in the external UI reference pack. New typed token module `lib/frontend/design/tokens.ts` and a rewritten `app/globals.css` covering surfaces, ink, brand ramp, the four-step competency rating ramp (ordinal → colour only, no labels), semantic status tints, typography scale, radii, elevation, layout measurements, focus-visible, disabled/invalid field states, skeleton and spinner primitives, reduced-motion and forced-colours support. New shared primitives: `surface.tsx` (Card, CardHeader, Eyebrow, AccentPanel, Divider), `badge.tsx` (Badge, Tag), `avatar.tsx`, `field.tsx` (Field, TextInput, TextArea, Select, SearchInput), `icon.tsx` (inline-SVG icon set, IconTile, IconButtonSurface). Restyled the existing shared shell and UI components — `portal-shell.tsx` (light sidebar, pink active navigation, header identity strip), `brand-mark.tsx`, `button.tsx`, `status-pill.tsx`, `page-heading.tsx`, `feedback-banner.tsx`, `state-panel.tsx` — and removed the dark page surface from `app/layout.tsx`. **No route, feature screen component, DTO, adapter contract, fixture, rating label, lifecycle rule or permission was touched, and no screen is claimed visually accepted.**
- **Files changed:** `app/globals.css`; `app/layout.tsx`; `app/(auth)/layout.tsx`; `components/brand/brand-mark.tsx`; `components/layout/portal-shell.tsx`; `components/ui/{button,feedback-banner,page-heading,state-panel,status-pill}.tsx`; new `components/ui/{avatar,badge,field,icon,surface}.tsx`; new `lib/frontend/design/tokens.ts`; new `tests/frontend/design-foundation.assertions.ts`; this frontend workstream log.
- **Tests and validation:** `node_modules/.bin/tsc.cmd --noEmit` — exit 0; `node_modules/.bin/eslint.cmd .` — exit 0; `npm.cmd run build` — exit 0, all 16 pre-existing routes built with no route added, removed or renamed; compiled `tests/frontend/design-foundation.assertions.ts` — exit 0, 46 assertions covering rating-ramp integrity, WCAG 2.2 AA contrast for every token pair, CSS/typed-token reconciliation, light-design invariants and absence of any external runtime asset; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0, including Management/Parent DOM privacy exclusions, with zero uncaught browser-console/runtime errors in both; `git diff --check` — exit 0. Diagnostic renders of seven existing surfaces at 1440 × 1024 were written outside Git to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F1\` with zero console errors; they are evidence only and constitute no screen acceptance.
- **Unresolved blockers:** No blocker to independent F1 review. Two items are recorded for later checkpoints, not resolved here. (1) **Contrast deviation from the frames.** White on the frames' primary pink `#ec4899` measures 3.53:1, short of the 4.5:1 that normal-size button labels require, and five badge text-on-tint pairs measured 3.84–4.49:1. The primary fill is therefore `#d6357a` (4.52:1) and the five tint text colours were deepened; hue preserved, luminance moved. Frame versus ratified accessibility requirement — the requirement wins and the deviation is recorded (A-045, `CLAUDE.md` persona 3.5). (2) **Parent per-dimension rating grid.** Frozen reference `33-parent-class-report` shows a "Performance Summary" of per-dimension rating tiles on a Parent surface, which `CLAUDE.md` §6 prohibits absolutely as a caught leak. F1 deliberately created no Parent-facing rating-tile primitive. The conflict belongs to `FRONTEND RECONSTRUCTION F15`, where the ratified rule governs and the frame does not.
- **Contract deviations requested:** none
- **Next action:** Independent F1 review and acceptance. `FRONTEND RECONSTRUCTION F2` (shared authentication shell) is not started; the provisional login presentation in `features/auth/login-presentation.tsx` still carries its pre-reference dark treatment and is reconstructed at F2/F3.

### 2026-08-06 — FRONTEND RECONSTRUCTION F2 (Shared Authentication Shell)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F2` — shared authentication shell. First checkpoint of the operator-authorized autonomous authentication batch (F2 → F3 → F10 → F13).
- **Starting commit:** `84b45189d7dfab30a93da8aa0b90a08fa2c165dc` (F1, accepted by the operator)
- **Ending commit:** The commit created by `feat(frontend): reconstruct shared authentication shell`
- **Status:** Ready for review
- **Scope completed:** Reconstructed the structural shell shared by the three frozen login references (AUTH-01 `546:370`, AUTH-02 `459:13`, AUTH-03 `546:413`), which are structurally identical and differ only in which segment reads as selected — the arrangement `GLOBAL_UI_RULES.md` §2 and `AUTH-01/screen.md` §13 expressly permit. New `components/auth/`: `auth-shell.tsx` (AuthShell column, AuthBackdrop decorative discs, AuthHeading, AuthFooterNote), `role-segmented-control.tsx` (the "Sign in as" selector as labelled navigation links with `aria-current`), `credential-fields.tsx` (EmailField, PasswordField with a type-only reveal control, CredentialOptionsRow). Retired the provisional dark authentication presentation identified at F1: `app/(auth)/layout.tsx` now renders the white reference canvas with the four pale corner discs. `features/auth/login-presentation.tsx` recomposed onto the shell. `BrandMark` gained a `size` prop for the authentication brand slot. **No screen is claimed visually accepted at F2.**
- **Files changed:** `app/(auth)/layout.tsx`; `app/(auth)/login/page.tsx`; `features/auth/login-presentation.tsx`; `components/brand/brand-mark.tsx`; new `components/auth/{auth-shell,role-segmented-control,credential-fields}.tsx`; new `tests/frontend/authentication-browser-smoke.mjs`; this frontend workstream log.
- **Tests and validation:** `tsc --noEmit` — exit 0; `eslint .` — exit 0; `npm run build` — exit 0, **route census unchanged at 16 routes**, none added, removed or renamed; new `node tests/frontend/authentication-browser-smoke.mjs` — exit 0 at the 1440 × 1024 reference viewport, covering shell composition, accessible labelling, retirement of the dark presentation, the type-only password reveal, per-role selection state, pre-authentication non-disclosure, disabled credentials, per-role fixture entry targets, and unknown/absent/malformed role fallback; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0; `git diff --check` — exit 0. Zero uncaught browser-console/runtime errors throughout. Diagnostic renders at 1440 × 1024 plus 900 × 1000 and 480 × 900 (no horizontal scroll) written outside Git to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F2\`.
- **Unresolved blockers:** None blocking review. Five frame-versus-governance deviations are **recorded, not resolved**: (1) the academy raster wordmark in all three frames has **no asset disposition** and `GLOBAL_UI_RULES.md` §8 forbids both copying it and re-drawing a logo ad hoc, so the approved in-repo mark occupies the brand slot — **operator disposition required**; (2) the primary action reads "Open <Role> fixture workspace", not "Sign in", because real Supabase Auth is delivered on `feat/48h-backend` and is not wired here — labelling it "Sign in" would claim an action it does not perform; (3) credential inputs remain disabled with an explicit note, preserving delivered behaviour and keeping a real password out of a field that goes nowhere; (4) "Remember me" renders unchecked where the frames show it checked, because a disabled checked box would imply session persistence that does not exist; (5) "Forgot password?" is inert, because recovery is a Supabase Auth flow not wired on this branch. Enabling the credential path is F16, not an authentication checkpoint.
- **Contract deviations requested:** none
- **Next action:** F3 (AUTH-01 Trainer Login) within the same authorized batch.

### 2026-08-06 — FRONTEND RECONSTRUCTION F3 (AUTH-01 Trainer Login)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F3` — AUTH-01 Trainer Login, node `546:370`, route `/login?role=trainer`. Second checkpoint of the authorized authentication batch.
- **Starting commit:** `07988a57239726d8b026165e2bc51c28da2147a5` (F2)
- **Ending commit:** The commit created by `feat(frontend): reconstruct trainer login`
- **Status:** Ready for review — **AUTH-01 proposed visually accepted**
- **Scope completed:** F2's shared shell already produced the Trainer frame's visual result, so F3 added the role-specific validation the plan requires to accept a screen rather than an empty restyle. New `tests/frontend/auth-reference-fidelity.assertions.ts` automates per-step contract item 2 for all three authentication references — SHA-256 against the validated pack, recorded byte size, native dimensions read from the PNG IHDR, a complete IEND chunk, and proof the three frames remain distinct by both digest and Figma node. The login region gained a role-specific accessible name (`Sign in — Trainer portal presentation`) so the three variants are distinguishable to assistive technology despite sharing one visible `Sign in` heading; this names a presentation, never an authority. Added keyboard-operability, real-reveal-button and visible-focus assertions, plus a cross-role reachability assertion.
- **Finding fixed during F3:** the cross-role assertion caught that `BrandMark`'s hardcoded `/trainer` destination made the Trainer workspace reachable from the Management and Parent logins. It granted no authority, but a pre-authentication screen should offer no route into any workspace except the selected role's own labelled entry. `BrandMark` gained `interactive={false}`, used only by the authentication screens; the portal shell's linked mark and its destination are unchanged.
- **Files changed:** `components/brand/brand-mark.tsx`; `features/auth/login-presentation.tsx`; `tests/frontend/authentication-browser-smoke.mjs`; new `tests/frontend/auth-reference-fidelity.assertions.ts`; this frontend workstream log.
- **Tests and validation:** compiled `auth-reference-fidelity.assertions.ts` — exit 0, AUTH-01 SHA-256 `b1ad24e4…56da1` confirmed identical to the validated pack at 1440 × 1024 and 95,496 bytes; `tsc --noEmit` — exit 0; `eslint .` — exit 0; `npm run build` — exit 0, **route census unchanged at 16**; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0 (10 check groups); `node tests/frontend/trainer-browser-smoke.mjs` — exit 0; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0; `git diff --check` — exit 0. Zero uncaught browser-console/runtime errors. Evidence at `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F3\`.
- **Destination dependency, recorded not resolved:** the expected physical-test destination for AUTH-01 is `/trainer/schedule`. **That route does not exist**, its treatment is `Operator decision required` (inventory §7.3, U-A5-1), and it is gated on F4. No destination route was created and no redirect policy was changed at F3; the fixture entry continues to target the delivered `/trainer` surface.
- **Unresolved blockers:** None blocking review. The five F2 frame-versus-governance deviations carry forward unchanged, of which the undispositioned academy wordmark still needs an operator asset decision.
- **Contract deviations requested:** none
- **Next action:** F10 (AUTH-02 Management Login) within the same authorized batch.

### 2026-08-06 — FRONTEND RECONSTRUCTION F10 (AUTH-02 Management Login)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F10` — AUTH-02 Management Login, node `459:13`, route `/login?role=management`. Third checkpoint of the authorized authentication batch.
- **Starting commit:** `b72752a88ed84144a135d19b64aea2c1658ceb95` (F3)
- **Ending commit:** The commit created by `feat(frontend): reconstruct management login`
- **Status:** Ready for review — **AUTH-02 proposed visually accepted**
- **Scope completed:** The shared shell already produced this frame's visual result, so F10 added measured visual-fidelity assertions and Management-specific governance assertions. **Measured geometry:** the rendered content column is asserted to be 400 px wide starting at x = 520 at the 1440 px reference viewport — the frozen frames' exact column geometry — and the seven landmarks (brand, role selector, heading, email, password, options row, primary action) are asserted to appear in the reference's vertical order. Vertical offsets are deliberately not asserted, because this reconstruction carries a governance notice the frames do not, which shifts everything below it; that is recorded rather than fudged. **Management-specific non-disclosure:** the pre-authentication screen is now asserted to expose none of `queue`, `pending review`, `final review`, `wording`, `correction`, `return to trainer`, `publish` or `submit` — no governed Management power may be hinted at before an identity exists. Added a shared-credential assertion covering `demo`, `shared login`, `shared credential`, `test account`, `sample password` and `default password` (A-015, A-046). Per-role forbidden-term lists were introduced for all three roles at the same time.
- **Functional behaviour preserved:** Management authentication and authorization behaviour is unchanged — none exists on this branch to change. The role query still grants nothing; the Management segment reading as selected mints no session, no membership and no permission. Authority remains server-derived from a real Supabase Auth identity and an active `centre_memberships` row.
- **Files changed:** `tests/frontend/authentication-browser-smoke.mjs`; this frontend workstream log.
- **Tests and validation:** compiled `auth-reference-fidelity.assertions.ts` — exit 0, AUTH-02 SHA-256 `fcc3db93…68483` confirmed identical to the validated pack at 1440 × 1024 and 95,584 bytes; `tsc --noEmit` — exit 0; `eslint .` — exit 0; `npm run build` — exit 0, **route census unchanged at 16**; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0; `git diff --check` — exit 0. Zero uncaught browser-console/runtime errors. Evidence at `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F10\`.
- **Destination dependency, recorded not resolved:** the expected physical-test destination for AUTH-02 is `/management/reports`, which **does exist** and is already canonical with no route mismatch. No route was created and no redirect policy was changed; the fixture entry targets the delivered `/management` surface, and wiring the real post-authentication destination is F16.
- **Unresolved blockers:** None blocking review. The five F2 frame-versus-governance deviations carry forward unchanged; the undispositioned academy wordmark still needs an operator asset decision.
- **Contract deviations requested:** none
- **Next action:** F13 (AUTH-03 Parent Login) within the same authorized batch.

### 2026-08-06 — FRONTEND RECONSTRUCTION F13 (AUTH-03 Parent Login)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F13` — AUTH-03 Parent Login, node `546:413`, route `/login?role=parent`. Final checkpoint of the authorized authentication batch.
- **Starting commit:** `52d26d08f6ae3439b9bf46c4561c17ed1f4acfb2` (F10)
- **Ending commit:** The commit created by `feat(frontend): reconstruct parent login`
- **Status:** Ready for review — **AUTH-03 proposed visually accepted**
- **Scope completed:** The shared shell already produced this frame's visual result, so F13 added the two assertion groups its acceptance criteria name directly. **Role-query escalation:** walking `parent → trainer → management → parent` is asserted to keep exactly one segment selected, move the entry point with the presentation, and — the sharpest form of A-046 — persist **nothing**: `localStorage`, `sessionStorage` and `document.cookie` are all asserted empty after every hop, so no session, role, credential or grant accumulates from switching the query. **Responsive usability:** at 1440, 1024, 900 and 480 px the page is asserted not to scroll horizontally, the primary action is asserted to stay on-screen with a ≥ 44 px touch target, and every role segment is asserted to remain visible and within the viewport. The Parent forbidden-term list (`child`, `linked`, `your children`, `progress`, `attendance`, `evidence`) was introduced at F10 and is enforced here: **no linked-child, report or attendance datum is exposed before authentication**, which is the Parent boundary (A-021, A-048) applied at its earliest surface.
- **Functional behaviour preserved:** Parent authentication and authorization behaviour is unchanged. Parent access remains submitted-canonical, view-only and limited to linked children — none of which this screen can grant, reach or hint at. Authority remains server-derived.
- **Files changed:** `tests/frontend/authentication-browser-smoke.mjs`; this frontend workstream log.
- **Tests and validation:** compiled `auth-reference-fidelity.assertions.ts` — exit 0, AUTH-03 SHA-256 `fcd4d4ed…3b85c` confirmed identical to the validated pack at 1440 × 1024 and 95,425 bytes; `tsc --noEmit` — exit 0; `eslint .` — exit 0; `npm run build` — exit 0, **route census unchanged at 16**; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0, 12 check groups; compiled `design-foundation.assertions.ts` — exit 0; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0; `git diff --check` — exit 0. Zero uncaught browser-console/runtime errors. Evidence at `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F13\`.
- **Destination dependency, recorded not resolved:** the expected physical-test destination for AUTH-03 is `/parent/reports`, which **does exist** and is already canonical with no route mismatch. No route was created and no redirect policy was changed; wiring the real post-authentication destination is F16.
- **Unresolved blockers:** None blocking review. The five F2 frame-versus-governance deviations carry forward unchanged; the undispositioned academy wordmark still needs an operator asset decision.
- **Contract deviations requested:** none
- **Next action:** Operator review of the authentication batch. F4 (05 Trainer Schedule) remains **blocked on the operator route decision** recorded at F0 (inventory §7.3, U-A5-1) and was not started.

### 2026-08-06 — F-01a (bounded WCAG 2.2 AA accessibility correction)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `F-01a` — bounded accessibility correction of the `bg-brand-600` white-label defect recorded at `AUTONOMOUS_48H_EXECUTION_TRACKER.md` §7.1 and risk register R-21. **Not a screen reconstruction; no screen is proposed accepted by this entry.**
- **Starting commit:** `d1883db9cd977f294747f4baad728d1be5bcebda` (F13)
- **Ending commit:** The commit created by `fix(frontend): use an accessible brand token for white-label actions (WCAG 2.2 AA)`
- **Status:** Ready for review
- **Scope completed:** `--color-brand-600` `#ec4899` gives **3.53:1** against white text — a live WCAG 2.2 AA failure for normal text (4.5:1 required). The **10 text-bearing sites across 9 components** enumerated in tracker §7.1 were moved from `bg-brand-600` to `bg-brand-700` `#d6357a` (**4.52:1**, passes), and their hover state from `hover:bg-brand-500` `#f472b6` (**2.65:1** — the hover state was failing *worse* than the rest state) to `hover:bg-brand-800` `#b02a63` (**6.26:1**, passes). This is the exact primary-action pair the F1 foundation already established in `components/ui/button.tsx`, `components/ui/state-panel.tsx` and `features/auth/login-presentation.tsx`, so the correction converges the feature components onto the accessible primitive rather than introducing a new treatment. **No token value in `app/globals.css` was changed**; no layout, spacing, copy, route or component structure was altered.
- **The two exclusions are deliberate and were verified untouched in the diff:** `components/brand/brand-mark.tsx:46` — the logotype glyph, WCAG-exempt, and operator ruling R-B8 requires the approved in-repository mark be used unaltered; `features/trainer/trainer-assessment.tsx:212` — a progress-bar fill carrying no text, which the Run B prompt expressly forbids altering as part of a global decorative-pink change. `grep -n bg-brand-600` after the change returns exactly those two sites and nothing else.
- **Functional behaviour preserved:** presentation only. No lifecycle transition, authorization path, DTO, port, fixture, route or copy string was touched. Trainer-approves-never-publishes, Management wording-only editing, the Parent submitted-canonical boundary and the nine-dimension gate are all unchanged.
- **Files changed:** `features/trainer/trainer-roster.tsx`; `features/trainer/returned-reports-queue.tsx`; `features/trainer/trainer-draft-generation.tsx`; `features/parent/parent-reports-list.tsx`; `features/parent/parent-dashboard.tsx`; `features/management/management-wording-editor.tsx`; `features/management/management-report-review.tsx`; `features/management/management-reports-queue.tsx`; `features/management/management-dashboard.tsx`; this frontend workstream log.
- **Tests and validation:** all 12 frozen `reference.png` SHA-256 digests verified against `FRONTEND_RECONSTRUCTION_PLAN.md` §5 **before and after** — all 12 unchanged and matching; `npx tsc --noEmit` — exit 0; `npx eslint .` — exit 0; `npm run build` — exit 0, **route census unchanged at 16**; compiled `tests/frontend/design-foundation.assertions.ts` — exit 0; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0; `git diff --check` — exit 0. **Zero uncaught browser-console/runtime errors** in all three browser suites. Contrast was **measured, not asserted from the token table**: every one of the 10 changed pairs was measured from live computed styles in the production build at **4.517:1** at rest and **6.255:1** on hover, and seven rendered instances were additionally measured in place on `/trainer/sessions/…/roster`, `/trainer/reports`, `/parent`, `/parent/reports`, `/management` and `/management/reports`. Measurements saved outside Git to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F-01a\`.
- **Unresolved blockers:** none.
- **Contract deviations requested:** none. **Recorded but out of scope, not fixed:** `text-brand-600` `#ec4899` is still used as text on white and on `brand-100` surfaces (for example `features/parent/parent-reports-list.tsx:70`, `features/management/management-reports-queue.tsx:97`, `features/trainer/trainer-draft-generation.tsx:163,173`, `features/trainer/trainer-report-review.tsx:286,366`, `features/trainer/trainer-dashboard.tsx:107`, `features/parent/parent-canonical-report.tsx:56`). Those pairs measure **3.53:1 on white** and **3.84:1 on `brand-100`**, which is a further AA failure for normal-size text. This checkpoint's owned paths were the 10 background sites only, so those foreground uses were deliberately left untouched and are reported for a separate authorization rather than silently swept in.
- **Next action:** Operator review. F4 (05 Trainer Schedule) remains blocked on the operator route decision (inventory §7.3, U-A5-1).

### 2026-08-06 — F-01b (bounded correction of the High verifier finding against F-01a)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `F-01b` — bounded correction of the **High** finding the independent verifier raised against `F-01a`. **Not a screen reconstruction; no screen is proposed accepted by this entry.**
- **Starting commit:** `6e8816e218d5b1b896abdf234be3657e3b6638e6` (F-01a)
- **Ending commit:** The commit created by `fix(frontend): restore Tailwind utility precedence on form controls (WCAG 2.2 AA)`
- **Status:** Ready for review
- **Scope completed:** `app/globals.css` declared `button, input, textarea, select { font: inherit; color: inherit; }` **unlayered**. Unlayered CSS outranks every `@layer` rule, and `@import "tailwindcss"` (Tailwind v4, layer order `theme, base, components, utilities`) emits every utility into `@layer utilities`. The reset therefore beat **every** colour and typography utility on **every** button, input, textarea and select in the application: the utility was generated and matched, then silently lost the cascade, so each control rendered its ancestor's colour and weight instead of its declared one. The rule is now declared **inside `@layer base`** — the same layer where Tailwind's own preflight declares the identical normalization (`node_modules/tailwindcss/preflight.css`) — so the reset still applies to any control that sets no utility, and any control that does set one now wins. **The reset was not deleted:** `font: inherit` / `color: inherit` on form controls is a deliberate normalization (browsers do not inherit into form controls) and is retained verbatim; only its cascade position changed. **No token VALUE was changed, no layer order was redefined, and `components/ui/button.tsx` needed no change** — the primary pair `text-white` on `bg-brand-700` was already correct in source and simply could not reach the DOM.
- **The defect was pre-existing, introduced at `84b45189d7dfab30a93da8aa0b90a08fa2c165dc`, not by F-01a's `6e8816e`** — but it defeated F-01a's load-bearing accessibility claim, so it is corrected before the lane continues.
- **Measured in the rendered DOM, production build, both named surfaces:** Management **"Approve & Submit"** (`/management/reports/report-birch/review`) and Trainer **"Save observation & generate draft"** (`/trainer/sessions/…/students/…/assess`, all nine rated, button enabled) each measured **`#1b2b4b` on `#d6357a` = 3.113:1 at 14 px / weight 400 before**, and **`#ffffff` on `#d6357a` = 4.517:1 at 14 px / weight 700 after** — a pass. Three further primary actions repaired identically: Trainer "Approve" on the review surface, "Approve" in the approval dialog, and "Approve for management review" inside it — all **3.113:1 → 4.517:1**. **Before the fix every button in the application computed to `font-weight: 400`; `font-bold` never applied anywhere.** Measurements are read from live computed styles with the effective background composited up the ancestor chain, not from the token table.
- **Collateral sweep — 122 controls across 16 surfaces, all three portals, measured before and after.** 23 unchanged, 99 changed, 0 added, 0 removed. **Zero inputs, selects or textareas changed at all**, because `.form-field` sets its own colour and remains authoritative. Changes are the declared design finally reaching the DOM: the 36 rating chips took their intended per-band ink at 12 px / 800 (**6.813–8.339:1**, all pass, previously flattened to `#1b2b4b` / 400); "Reset fixture" took `text-small` + `font-bold` (13 px / 700, **15.598:1**, unchanged pass — it had been inheriting white from its dark ancestor rather than applying `text-white`); "Cancel" and "Return assessment concern" took `font-bold` with colour unchanged.
- **Six controls moved below 4.5:1 and every one was individually adjudicated; none is a WCAG 2.2 AA regression.** (1–3) The **password-reveal control** on all three login screens took its declared `text-ink-muted` `#8a93a8` (**3.079:1 on white**, was `#33405c` by inheritance). Its visible content is the `EyeGlyph` **inline SVG**; the string "Show password" is `sr-only` and therefore has **no** contrast requirement. The applicable criterion is **SC 1.4.11 Non-text Contrast at 3:1**, which **3.079:1 meets** — the harness only flagged it because `textContent` includes visually-hidden text. Margin is thin and is recorded for a future foundation pass; `components/auth/credential-fields.tsx` is outside this checkpoint's owned paths and was **not** touched. (4–6) Three **`disabled`** controls took their declared inert treatment: Trainer "Save observation & generate draft" and Management "Save wording changes" at `disabled:text-ink-subtle` `#a6aec0` on `disabled:bg-neutral-soft` `#f1f3f8` (**2.004:1**), and the roster's "Absent · assessment unavailable" at `text-slate-500` on `bg-slate-100` (**4.349:1**). **SC 1.4.3 expressly exempts inactive user-interface components**, and this is exactly the "visibly inert treatment" `button.tsx` documents — the checklist-gated and validation-gated actions now actually *read* as disabled, which they did not before. Recorded, not fixed.
- **Functional behaviour preserved:** presentation only. No lifecycle transition, authorization path, DTO, port, fixture, route, component structure or copy string was touched. Trainer-approves-never-publishes, Management wording-only editing, the Parent submitted-canonical boundary and the nine-dimension gate are all unchanged. Class Grade vocabulary untouched; no keyword replacement of any kind was performed (A-054). `components/brand/brand-mark.tsx` and `features/trainer/trainer-assessment.tsx:212` were **not** touched, as required.
- **Files changed:** `app/globals.css`; this frontend workstream log.
- **Tests and validation:** all 12 frozen `reference.png` SHA-256 digests verified against `FRONTEND_RECONSTRUCTION_PLAN.md` §5 **before and after** — 12/12 match, 0 mismatch, exit 0 both times; `npx tsc --noEmit` — exit 0; `npx eslint .` — exit 0; `npm run build` — exit 0, **route census unchanged at 16 application routes** (`/`, `/login`, `/management`, `/management/reports`, `/management/reports/[reportId]/edit`, `/management/reports/[reportId]/review`, `/parent`, `/parent/reports`, `/parent/students/[studentId]/sessions/[sessionId]/report`, `/trainer`, `/trainer/reports`, `/trainer/reports/[reportId]/edit`, `/trainer/reports/[reportId]/generate`, `/trainer/reports/[reportId]/review`, `/trainer/sessions/[sessionId]/roster`, `/trainer/sessions/[sessionId]/students/[studentId]/assess`, plus `/_not-found`) — none added, removed or renamed; compiled `tests/frontend/design-foundation.assertions.ts` — exit 0, all assertions pass; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0; `git diff --check` — exit 0. **Zero uncaught browser-console/runtime errors** in all three browser suites and in both measurement passes. The before-measurement was taken against a production build of the **pre-fix** stylesheet and the after-measurement against a production build of the **post-fix** stylesheet, using the **identical** harness. Measurements, the full 122-control before/after diff and diagnostic renders were written **outside Git** to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F-01b\`.
- **Unresolved blockers:** none.
- **Contract deviations requested:** none. **Recorded but out of scope, not fixed:** (a) `app/globals.css` still declares `*`, `body`, `h1`–`h4`, `:focus-visible`, `::selection`, `.card`, `.panel`, `.form-field` and the loading primitives **unlayered**, so those also outrank Tailwind utilities — most consequentially `h1, h2, h3, h4 { color: #1b2b4b }`, which would defeat a `text-white` heading on a dark accent panel. This checkpoint's owned path was **the form-control reset only**, so the remaining unlayered rules were deliberately left untouched and are reported for a separate authorization rather than silently swept in. (b) The `text-brand-600` foreground uses recorded at F-01a (3.53:1 on white, 3.84:1 on `brand-100`) are **unchanged and still failing**; F-01b did not widen into them either. (c) `features/trainer/trainer-roster.tsx` uses Tailwind default-palette classes (`bg-slate-100`, `text-slate-500`) rather than project tokens — a foundation-convergence item for that screen's own checkpoint.
- **Next action:** Operator/verifier review of F-01b. F4 (05 Trainer Schedule) remains blocked on the operator route decision (inventory §7.3, U-A5-1).

### 2026-08-06 — FRONTEND RECONSTRUCTION F11 (29 Management Reports)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F11` — screen `29` Management Reports, node `527:170`, route `/management/reports`, physical-test flow order 8. Executed as operator checkpoint **F-11**.
- **Starting commit:** `69ca3e59fb668400f6f7bd4db22c65f12e642430` (F-01b)
- **Ending commit:** The commit created by `feat(frontend): reconstruct management reports queue`
- **Status:** Ready for review — **screen `29` proposed visually accepted**
- **Reference integrity:** `29-management-reports/reference.png` SHA-256 `eddda3b1…54c19` verified **before** any work began and **unchanged after** — 1440 × 1160, 98,030 bytes, byte-identical to `FRONTEND_RECONSTRUCTION_PLAN.md` §5.
- **Scope completed:** The queue was rebuilt from the frozen frame: page title **"Reports"** with its oversight subtitle, the frame's **`Filter:` strip** (status chip + two inert scope chips + right-aligned student search), and **one white card holding one report table** — Student · Session · Status · Action — replacing the previous card-per-row list. The status chip drives the two **ratified `?status=` compatibility aliases**, which continue to work as query variants of ID 29 rather than separate routes; the `?preview=empty` diagnostic is preserved. Search filters **only the rows already returned by the governed projection** and reaches no other report. Loading, empty, no-search-match, failed, unavailable and denied states are all built.
- **Privacy boundary — the point of this screen, and it is proved, not asserted:** row actions are decided **per row by that row's own status**, through a single `ROW_PRESENTATION` table carrying an explicit `exposesContent` flag. There is deliberately **no shared generic "view report" handler** — `CLAUDE.md` §6 requires every such handler to check status independently. `trainer_approved` → **"Review ›"** → the management final-review surface, the one permitted pre-submission read (A-038). `needs_edit` and `draft_ready` → **"Send Reminder to Trainer"**, and **no content link of any kind**. Measured in the rendered production DOM: `?status=trainer_approved` contains a Review link and contains "Send Reminder to Trainer" = **false**; `?status=needs_edit` contains "Send Reminder to Trainer" = **true** and contains a Review link = **false**. **No trainer note, raw per-dimension rating, checklist internal, approval internal or content hash is rendered on any row, `trainer_approved` included.** The only correction data shown are the issue scope, the open/resolved flag and the **Management-authored** correction reason — all three already carried by the governed DTO and already surfaced before this checkpoint.
- **Status-union widening — exactly one line:** `ManagementQueueRowDto.status` in `lib/frontend/contracts/physical-test.ts` widened from `"trainer_approved" | "needs_edit"` to `"trainer_approved" | "needs_edit" | "draft_ready"`, precisely as the plan's F11 section requires. This **adds no status to the ratified eight** (`draft_ready` is already one, A-036), **creates no lifecycle transition**, and **grants Management no access to `draft_ready` content** — a `draft_ready` row here is correction-tracking **metadata only**: that returned work has been corrected but not yet reapproved. **The rating union on lines 1–8 was not touched** and still declares the superseded `emerging`/`developing`/`secure`/`advanced`, which is the correct pre-V3 state and belongs to **F6**. The **stop condition was evaluated explicitly and did not fire.**
- **Functional behaviour preserved:** the canonical route, both governed projections (`listManagementPendingReviews`, `listManagementCorrectionTracking`), the `?status=` aliases, the `/management/reports/[reportId]/review` destination and every resource state. No port method, fixture, route, backend path or permission was added or changed. Trainer-approves-never-publishes, Management wording-only editing and the Parent submitted-canonical boundary are untouched.
- **Not rating-bearing:** screen `29` surfaces no competency-rating vocabulary (`screen.md` §8), so F11 carries **no F6 dependency**. No Class Grade artefact was touched and **no keyword replacement** over `advanced`/`secure`/`emerging`/`beginning`/`mastering`/`mastered` was performed anywhere (A-054).
- **Five frame-versus-governance deviations — recorded, not silently resolved:** **D1** the frame's "Approved"/"Needs approval" chips are Figma mock data, not the ratified lifecycle — `approved` is transient-in-transaction and **never commits** (A-036), so it can never label a queue row; ratified statuses win. **D2** the frame gives every row a content-bearing action; governance forbids that pre-approval, so those rows get the reminder control instead. **D3** the frame's **Class, Lesson and Trainer** columns have no governed field behind them and are **omitted rather than fabricated**; the "Submitted" column is rendered as **"Session"** because a `trainer_approved` report has no submission timestamp and labelling it "Submitted" would be false. **D4** column headers, search placeholder and glyph use `#5f6880` and the two explanatory notes `#33405c`, against the frame's lighter greys which measure **2.043:1** and **3.079:1** — below the 4.5:1 AA floor; hue preserved, luminance moved, AA wins (A-045, persona §3.5), the same trade F1 recorded for `STATUS_TOKENS`. **D5** the frame's six-item navigation rail is `components/layout/portal-shell.tsx`, outside the owned paths and untouched; its remaining items depend on post-48-hour deferred screens (A-044).
- **Four dependencies recorded, none invented:** (1) **term and class filter fields** — the projection carries neither, so both chips render **inert with a visible, programmatically associated reason** rather than being faked client-side or silently dropped; (2) **Trainer-reminder delivery** — `CLAUDE.md` §6 mandates the "Send Reminder to Trainer" action for pre-approval rows, but no reminder exists on `PhysicalTestPort`, so the control carries its **mandated label** and is **disabled with a stated reason**, giving the governed affordance without inventing behaviour or shipping a dead control; (3) **Class/Lesson/Trainer columns** (D3); (4) **`draft_ready` rows are not yet produced** — the fixture's correction-tracking projection filters on `status === "needs_edit"`, and the fixture is outside the owned paths, so no such row reaches the queue today; the component handles it correctly and defensively, and producing one is F16 adapter work.
- **Latent F1 primitive defect — recorded, not fixed:** `components/ui/field.tsx`'s **`Select` and `SearchInput` had never been rendered anywhere** in the application before this checkpoint. Both compose `.form-field`, whose `background` and `padding` **shorthands reset the very background-repeat / background-position / padding-left utilities those primitives rely on** for their chevron and icon inset — and `.form-field` is still declared **unlayered**, which F-01b already recorded as outranking every Tailwind utility. Consuming them produced a visibly broken filter strip (chevrons tiled across the full row, search text under the glyph). Both files are outside this checkpoint's owned paths, so **neither was modified**; the chip and search box were built locally from utilities, and the defect is reported for a separate foundation authorization.
- **Accessibility:** real `<table>` with `<caption>` and `scope="col"` headers; search field and all three filter chips carry programmatically associated labels; both inert affordances carry `aria-describedby` pointing at a visible stated reason; **status is carried by text, never colour alone**; **no horizontal page scroll at 1024 px** (measured true), the table scrolling inside its own container. Contrast was **measured from live computed styles in the production build** across 60 text nodes and three states: **every enabled element inside this screen's owned surface passes.** The 16 residual failures are all outside the owned paths or exempt — the shell nav rail at 3.079:1 (`portal-shell.tsx`, **pre-existing**), and three **`disabled`** controls at 2.043:1, which **SC 1.4.3 expressly exempts** as inactive user-interface components, the same adjudication F-01b recorded. To avoid *introducing* a failure, the page subtitle is rendered locally at `#33405c` rather than through `PageHeading`'s `description` prop, which resolves to `text-ink-muted` `#8a93a8` (2.828:1) — the shared primitive was left untouched and the failure recorded.
- **Files changed:** `features/management/management-reports-queue.tsx`; `lib/frontend/contracts/physical-test.ts` (**the status-union line only**); this frontend workstream log.
- **Tests and validation:** `29-management-reports/reference.png` SHA-256 verified **before and after — unchanged**; `npx tsc --noEmit` — exit 0; `npx eslint .` — exit 0; `npm run build` — exit 0, **route census unchanged at 16 application routes** plus `/_not-found` — none added, removed or renamed; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0; `git diff --check` — exit 0. **Zero uncaught browser-console/runtime errors** in every browser run. Diagnostic renders of the pending, correction-tracking and empty states at **1440 × 1160** plus a narrower **1024 × 900** desktop breakpoint, and the full contrast measurement set, were written **outside Git** to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F11\`. Synthetic fixture data only.
- **Unresolved blockers:** none. The four dependencies above are recorded for the operator, not blocking.
- **Contract deviations requested:** none.
- **Next action:** Operator review of F11. **F12 (19 Management Student Report) is still blocked by F6**, which is itself blocked on Backend V2 or a coordinated operator authorization. F4 (05 Trainer Schedule) remains blocked on the operator route decision (inventory §7.3, U-A5-1).

### 2026-08-06 — FRONTEND RECONSTRUCTION F14 (32 Parent Reports)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F14` — screen `32` Parent Reports, node `533:180`, route `/parent/reports`, physical-test flow order 11. Executed as operator checkpoint **F-14**.
- **Starting commit:** `77abff4e1c5dc59381a8c2b81fbbe6ee932408de` (F11)
- **Ending commit:** The commit created by `feat(frontend): reconstruct parent reports list`
- **Status:** Ready for review — **screen `32` proposed visually accepted**
- **Reference integrity:** `32-parent-reports/reference.png` SHA-256 `90e368c1…70aea` verified **before** any work began and **unchanged after** — 1440 × 1120, 73,658 bytes, byte-identical to `FRONTEND_RECONSTRUCTION_PLAN.md` §5.
- **Scope completed:** The list was rebuilt from the frozen frame: page title **"Reports"** with its subtitle, a top-right **"Viewing &lt;learner&gt;"** affordance, an **"All Reports"** section heading, and report rows composed as **tinted document tile · title · meta line · right-aligned primary "View"**. The previous provisional eyebrow-and-heading block and card-per-row list were replaced, and this screen's legacy `navy-*` / `text-2xl` / `text-sm` utilities were converted to the F1 token scale. Loading, empty (`?preview=empty`), failed, unavailable and denied states are all built. **No route, DTO, port method, permission or fixture was added or changed** — the governed R-9 projection, the live-link reachability check and the `/parent/students/[studentId]/sessions/[sessionId]/report` destination are untouched.
- **GOVERNANCE OVERRODE THE FROZEN SCREENSHOT — this is the primary deliverable of F-14 (operator ruling R-B6).** **Deviation D1:** the frozen reference draws an **AGGREGATE RATING CHIP on every report row** ("Mastering", "Developing", tinted teal and amber, beside each row title). **It is deliberately NOT implemented.** An aggregate chip is not a softer form of a rating grid — it is a rating disclosure with fewer digits: it states assessment substance the Parent boundary does not carry, and on a four-value ordinal scale it also narrows the underlying per-dimension grid. A Parent receives the **submitted canonical narrative only**. Prohibited on this surface and absent from it: aggregate rating chips, per-dimension ratings, observations, correction history, content hashes, version metadata and audit internals (`CLAUDE.md` §6; A-021; A-038; A-048; `screen.md` §6 and §8; `GLOBAL_UI_RULES` §5). The frame was used for **shell, spacing, typography and layout, and for nothing else**. Figma never bypasses governance: the rule wins and the divergence is recorded, never silently resolved (A-045). **The stop condition — "an aggregate rating chip would be implemented → BLOCKED" — was evaluated explicitly and did not fire, because no such chip was implemented.**
- **Required negative assertion — load-bearing privacy evidence, and it is proven falsifiable rather than merely green.** `tests/frontend/three-role-browser-smoke.mjs` now carries `assertNoRatingTokenRendered()`, run at **four** parent surfaces: the screen 32 list before any submission, the Parent home surface, the screen 32 list **after a real end-to-end Management submission** (the exact state the frame draws its chip in), and the screen 33 canonical report detail. It proves **no competency-rating token renders on any parent surface in either vocabulary** — the four ratified A-049 labels (`beginning`/`developing`/`mastering`/`mastered`) and the four superseded labels the frontend still declares (`emerging`/`developing`/`secure`/`advanced`). It is **deliberately not a bare-word prose regex**: Amendment 006 **A-052 expressly prohibits one**, because "at the beginning of the session" and "has mastered maintaining eye contact" are legal parent-facing English. What it detects is exactly the form A-052 authorises — an **isolated raw label presented as a rendered value**: any **leaf** element whose entire text is one token, plus any element carrying a rating-bearing data attribute. **Class Grade is a different vocabulary (A-054)** and an element that legitimately renders one marks itself `data-vocabulary="class-grade"` and is skipped; occurrences are classified by **actual context, never by keyword**. **Falsifiability was demonstrated, not assumed:** the reference's own aggregate chip was injected into the live production DOM and the identical guard **detected it** (`span="mastering"`); a legal-prose probe was **not** flagged; a context-marked Class Grade `Advanced` was **not** flagged. Recorded to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F-14\f14-evidence.json`.
- **Four further frame-versus-governance deviations — recorded, not silently resolved:** **D2** the frame's row title is a lesson/topic name and its meta line carries class grade, class module, lesson number and trainer name; the governed `ParentReportListItemDto` carries **none** of those fields, so they are **omitted rather than fabricated** and recorded as a dependency. **D3** the frame places the child affordance in the page header rail, which is `components/layout/portal-shell.tsx` — outside the owned paths and untouched — so the affordance renders at the top right of the page content instead. **D4** the frame's left rail (Overview / Calendar / Reports / Logout) is that same unowned shell; its remaining items depend on deferred post-48-hour screens (A-044). **D5** the page subtitle is rendered locally rather than through `PageHeading`'s `description` prop, which resolves to `text-ink-muted` `#8a93a8` and fails the 4.5:1 AA floor — the shared primitive was left untouched and the failure recorded, the same trade F11 made.
- **Two dependencies recorded, none invented:** (1) **report/lesson title, class grade, class module and trainer name** are not carried by the governed Parent projection — nothing was faked client-side, stubbed as if real or inferred from the frame (D2); (2) the fixture links **exactly one child**, so the multi-child branch of the "Viewing" affordance is built and typechecked but not exercised by fixture data — producing a second linked child is F16 adapter/fixture work, and the fixture is outside the owned paths.
- **The linked-child affordance is a presentation control over live links only.** It is derived from the rows the governed projection **already returned**, so it can never name a student the authenticated Parent has no live link to, and it reaches no other student, report or version — the same "narrows what the projection returned" pattern F11 used for search. It defaults to all linked learners, so no report is hidden by default.
- **Functional behaviour preserved:** the canonical route, `port.listParentSubmittedReports()`, the live-link reachability check, the report destination, the `?preview=empty` diagnostic and every resource state. Parent access remains **submitted-canonical and view-only**; there is **no edit affordance** on this surface. Trainer-approves-never-publishes and Management wording-only editing are untouched. The three-role suite continues to prove a trainer-approved-but-unsubmitted report **stays invisible to the Parent**.
- **Not rating-bearing:** screen `32` surfaces no competency-rating vocabulary (`screen.md` §8), so F14 carries **no F6 dependency**. The rating union in `lib/frontend/contracts/physical-test.ts` still declares the superseded `emerging`/`developing`/`secure`/`advanced` and was **not touched** — the correct pre-V3 state, which belongs to F6. No Class Grade artefact was touched and **no keyword replacement** over `advanced`/`secure`/`emerging`/`beginning`/`mastering`/`mastered` was performed anywhere (A-054).
- **Accessibility:** row titles are `h2` under the page `h1` so the outline does not skip; the list is a `section` labelled by its "All Reports" heading; the child affordance is a real `select` with a programmatically associated `label` — and a plain labelled text pair when only one child is linked, so no `label` ever points at a non-labelable element; each "View" action carries an `sr-only` student-and-date suffix so its accessible name is unique; the primary action keeps a ≥ 44 px target; the empty state keeps `role="status"`. Contrast was **measured from live computed styles in the production build** across the **8** text nodes of the owned surface: **8/8 pass, 0 failures, 0 exemptions claimed**. **No horizontal page scroll at 1440, 1024 or 480 px** — measured true at all three.
- **Files changed:** `features/parent/parent-reports-list.tsx`; `tests/frontend/three-role-browser-smoke.mjs` (the negative assertion and its three call sites); this frontend workstream log.
- **Tests and validation:** `32-parent-reports/reference.png` SHA-256 verified **before and after — unchanged**; `npx tsc --noEmit` — exit 0; `npx eslint .` — exit 0; `npm run build` — exit 0, **route census unchanged at 16 application routes** plus `/_not-found` — none added, removed or renamed; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0, now **10 check groups** including the R-B6 rating-token check; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0; `git diff --check` — exit 0. **Zero uncaught browser-console/runtime errors** in every browser run. **No dependency, no test runner, no `package.json` or `package-lock.json` change of any kind** (R-B7). Diagnostic renders at **1440 × 1120** (the reference's native dimensions), the empty state, and narrower **1024 × 900** and **480 × 900** breakpoints, plus the contrast and falsifiability measurements, were written **outside Git** to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F-14\`. Synthetic fixture data only.
- **Unresolved blockers:** none. The two dependencies above are recorded for the operator, not blocking.
- **Contract deviations requested:** none. **One owned-path note:** the checkpoint's stated owned paths are the feature component and this screen's `implementation-notes.md`, but the required negative assertion cannot live in either — it is a browser-DOM assertion — so `tests/frontend/three-role-browser-smoke.mjs` was edited as the assertion's only possible home, additively and with no existing assertion weakened or removed. **Recorded but out of scope, not fixed:** the `text-brand-600`-on-white foreground failures recorded at F-01a no longer occur on this screen (the eyebrow that used it was replaced), but they remain open elsewhere; `PageHeading`'s `description` prop remains an AA failure for any consumer (D5); the unlayered `app/globals.css` rules recorded at F-01b are unchanged.
- **Next action:** Operator review of F14. **F15 (33 Parent Class Report)** is the next parent screen and was **not** started — one screen, one checkpoint, one stop. **F12 (19 Management Student Report) is still blocked by F6**, which is itself blocked on Backend V2 or a coordinated operator authorization. **F4 (05 Trainer Schedule) remains blocked** on the operator route decision (inventory §7.3, U-A5-1).

### 2026-08-06 — FRONTEND RECONSTRUCTION F4 (05 Trainer Schedule)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F4` — screen `05` Trainer Schedule, node `591:9`, canonical route `/trainer/schedule`, physical-test flow order 2. Executed as operator checkpoint **F-04**.
- **Starting commit:** `50e0ee286d7083bd6dbe201bca56009d73ae65c4` (F14)
- **Ending commit:** The commit created by `feat(frontend): reconstruct trainer schedule`
- **Status:** Ready for review — **screen `05` proposed visually accepted**
- **Reference integrity:** `05-trainer-schedule/reference.png` SHA-256 `d2d58b16…4ff2ceb` verified **before** any work began and **unchanged after** — 1675 × 1155, 90,168 bytes, byte-identical to `FRONTEND_RECONSTRUCTION_PLAN.md` §5 and to the tracker checksum table.
- **THE ROUTE DECISION — operator ruling R-B1 executed exactly as written, and it is the headline of this checkpoint.** U-A5-1 (inventory §7.3), open since the route reconciliation, is resolved: **`/trainer/schedule` is CREATED as the canonical Trainer entry route**, and **`/trainer` is PRESERVED as a compatibility redirect onto it** — `app/(portals)/trainer/page.tsx` was **converted, never deleted**, and now issues a server redirect. Measured on the production build: `GET /trainer` → **307 → `/trainer/schedule`**; `GET /trainer/schedule` → **200**. **No working route was silently deleted**, and every delivered surface that links to `/trainer` — the AUTH-01 login entry target, `StatePanel`'s default home, the shell reset action — still lands somewhere real. This is the same treatment inventory §7.2 already ratifies for the deferred screen `01` (`/trainer/dashboard`): *"Preserve existing route as redirect."*
- **ROUTE CENSUS MOVE — RECORDED, as the stop condition requires. 16 → 17 application routes plus `/_not-found`.** Exactly **one** route was added: **`/trainer/schedule`**. **None was removed, renamed or restructured** — `/trainer` is still in the census, still builds, still responds. The move is cross-referenced in `UI_REFERENCE_FINAL_MVP/CHANGE_LOG.md`.
- **Scope completed:** Screen `05` was built from the frozen frame: page title **"Schedule"** with its "Your classes, sessions and meetings" subtitle and a **"Search schedule"** field; a calendar card whose toolbar carries the **month label and month picker**, the **Day / Week / Month** switch with **Month selected**, and **"Add Agenda"**; the **full Sunday-first month grid** with borrowed neighbouring-month days muted and session chips carrying Class Grade · Class Module and start time; and the **"Schedule Details" side panel** with its close control, date line, `Class` badge, session card and primary action. Loading, empty (`?preview=empty`), no-search-match, no-day-selected, no-session-on-day, failed and unavailable states are all built.
- **THE CALENDAR IS A PROJECTION, and that is proved by construction, not asserted.** `features/trainer/trainer-schedule-projection.ts` is a **pure, side-effect-free date projection** over the rows `PhysicalTestPort.listTrainerSessions()` already returns. **No event table, agenda record, calendar entity, second store, persistence or mutation exists anywhere in this checkpoint** (A-016, A-047, `screen.md` §6 "Prohibited invention"). Because the only input is that governed projection, **the surface cannot name a Class Session the trainer is not assigned to** — the assignment check stays server-proved upstream and is neither duplicated nor re-implemented here. Search, the month picker and the Day/Week/Month switch **narrow** what the projection returned; none of them can widen it. All date arithmetic is UTC, so a viewer's timezone can never move a session onto a neighbouring day, and the default focus month is derived from the **earliest assigned session** rather than from "today", so the surface renders identically in every environment and every capture.
- **Governed functionality preserved:** the existing session-selection function that lived on the `/trainer` landing surface is preserved on the Schedule surface — each session card opens the same governed roster at `/trainer/sessions/[sessionId]/roster` (`screen.md` §6). **No port method, no DTO, no fixture row, no permission and no backend path was added or changed.** `lib/frontend/fixtures/` and `lib/frontend/contracts/` are **untouched** — the date projection needed no fixture data that did not already exist. Trainer-approves-never-publishes, Management wording-only editing and the Parent submitted-canonical boundary are untouched; the three-role suite still proves a trainer-approved-but-unsubmitted report stays invisible to the Parent.
- **Two frame-versus-governance deviations — recorded, not silently resolved (A-045).** **D1 "Add Agenda"** — the frame draws a primary create affordance. **No create-session path exists on `PhysicalTestPort`, and creating a Class Session is a governed MANAGEMENT action (A-019), not a Trainer one.** The control therefore keeps the frame's exact label and is rendered **`disabled` with a visible, programmatically associated reason**, exactly as F-11 handled "Send Reminder to Trainer". Nothing was invented, stubbed as if real or faked client-side. **D2 "Start Class"** — the frame's primary detail action implies a **session-lifecycle transition**. The session-lifecycle enum is **deferred and unratified**, and `CLAUDE.md` §6.1 / A-026 say plainly *"do not invent a placeholder enum"*. A control named "Start Class" that starts nothing would be a false claim of state change, so it is **relabelled "Open Class Roster"** — the governed action it actually performs, and the behaviour that already existed. Figma is authoritative for microcopy; **where microcopy asserts an unratified transition, the ratified rule wins and the divergence is recorded.**
- **Four dependencies recorded, none invented:** (1) the frame's Schedule Details panel shows a **room/location** ("Studio 2") — `TrainerSessionSummaryDto` carries no location field, so it is **omitted rather than fabricated**; (2) the frame shows **"Main:" and "Assist." trainer names** — the projection carries no assignment-name field, and rendering staff identities here was not going to be guessed, so both are omitted; (3) **session creation** has no Trainer-reachable governed path (D1); (4) the frame's left rail also lists **My Classes** and **Students**, which depend on the *"Trainer-scoped class and student projections"* recorded missing for screens `02` and `04` (inventory §8.2) — those items are **not** added, because a nav item pointing at nothing is a dead control. **The frame's Day and Week views were NOT faked**: both are real projections of the same governed rows, so no view in the switch is a dead control.
- **Not rating-bearing:** screen `05` surfaces no competency-rating vocabulary (`screen.md` §8), so F4 carries **no F6 dependency**. The rating union in `lib/frontend/contracts/physical-test.ts` still declares the superseded `emerging`/`developing`/`secure`/`advanced` and was **not touched** — the correct pre-V3 state, which belongs to F6. The **Class Grade** vocabulary this screen does render is a **different, unchanged vocabulary** (A-054): `Beginner` / `Intermediate` / `Advanced` are rendered verbatim, the Schedule Details value is marked `data-vocabulary="class-grade"` so a future rating-token guard classifies it by **actual context**, and **no keyword replacement** over `advanced`/`secure`/`emerging`/`beginning`/`mastering`/`mastered` was performed anywhere.
- **Accessibility:** the month grid is a real `<table>` with an `sr-only` `<caption>` and `scope="col"` weekday headers; each in-month day is **one** keyboard-operable control carrying a composed accessible name ("Wednesday, 5 August 2026. Beginner Storytelling Foundations, 2:00 PM – 3:30 PM"), with the selected day marked `aria-current="date"` **and** by a filled treatment, never by colour alone; the view switch is a labelled `role="group"` of `aria-pressed` buttons; the search field and month picker have programmatically associated labels; "Add Agenda" carries `aria-describedby` pointing at its visible reason; empty and no-match states carry `role="status"`; the heading outline is `h1 → h2 → h3` with no skip. **A real accessibility defect was found and fixed inside this checkpoint:** the `sr-only` weekday names were **absolutely positioned inside the horizontally scrolling table**, escaping the scroll container's clipping and extending `documentElement.scrollWidth` to **688 px at a 480 px viewport** — a genuine horizontal **page** scroll, which `GLOBAL_UI_RULES` §6 forbids. The full weekday name is now the header's `aria-label`, which names the column without adding a laid-out box; re-measured, `documentElement.scrollWidth` is **465 ≤ 480**. **No horizontal page scroll at 1675, 1024 or 480 px — measured true at all three**, with the wide grid scrolling inside its own container. Contrast was **measured from live computed styles in the production build** across **92** text nodes: **every owned-surface node passes.** The 5 residual failures are all `components/layout/portal-shell.tsx` at 2.828:1 — the identity sub-label and the fixture footer, **pre-existing and outside the owned surface**; the 2 remaining are the **`disabled`** "Add Agenda" at 2.043:1, which **SC 1.4.3 expressly exempts** as an inactive user-interface component, the same adjudication F-01b and F-11 recorded. The frame's very light grey for borrowed neighbouring-month dates measured **2.043:1**; a date is meaningful text, so the AA floor won over the frame — hue preserved, luminance moved, recorded here (A-045, persona §3.5).
- **Files changed:** `app/(portals)/trainer/schedule/page.tsx` (**new route**); `app/(portals)/trainer/page.tsx` (**converted to the compatibility redirect — not deleted**); `features/trainer/trainer-schedule.tsx` (**new**); `features/trainer/trainer-schedule-projection.ts` (**new**); `features/trainer/trainer-dashboard.tsx` (**comment only** — retained for the deferred screen `01`, not deleted); `features/trainer/trainer-roster.tsx`, `features/trainer/trainer-assessment.tsx`, `features/trainer/trainer-report-review.tsx`, `features/trainer/trainer-draft-generation.tsx`, `features/trainer/returned-reports-queue.tsx` (**the "Dashboard" / "Return to dashboard" links repointed to `/trainer/schedule`**, so no delivered surface advertises a destination that now redirects away); `components/layout/portal-shell.tsx` and `components/ui/icon.tsx` (**outside the stated owned paths — see the deviation note below**); `tests/frontend/trainer-browser-smoke.mjs`; this frontend workstream log. **The fixture module, the DTO contracts, `lib/frontend/physical-test-port.ts`, `package.json` and `package-lock.json` were NOT touched.**
- **Tests and validation:** `05-trainer-schedule/reference.png` SHA-256 verified **before and after — unchanged**; `node_modules/.bin/tsc --noEmit` — exit 0; `node_modules/.bin/eslint .` — exit 0; `npm run build` — exit 0, **route census 16 → 17** application routes plus `/_not-found`, **`/trainer/schedule` added, none removed or renamed**; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0, now **8 check groups** including the new route, the `/trainer` redirect, the month projection, the inactive "Add Agenda", day selection, the view switch and the empty state; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0; `git diff --check` — exit 0. **All three browser suites were run against a PRODUCTION build (`next start`), never a cold dev server.** One process hazard was hit and is recorded so it is not re-learned: rebuilding `.next` underneath a **running** `next start` invalidates the chunk hashes the live server is still serving, and the client silently never boots — the server must be restarted after every rebuild, and every result above is from a server started on the final build. **Zero uncaught browser-console/runtime errors** in every run. **No dependency, no test runner, no `package.json` or `package-lock.json` change of any kind** (R-B7). Seven diagnostic renders — month, month-with-details, week, day and empty at the reference **1675 × 1155**, plus **1024 × 900** and **480 × 900** — and the full contrast and overflow measurements were written **outside Git** to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F-04\`. Synthetic fixture data only.
- **Unresolved blockers:** none. The four dependencies above are recorded for the operator, not blocking.
- **Contract deviations requested:** none — but **two owned-path notes, declared rather than buried.** (1) `components/layout/portal-shell.tsx` is outside the stated owned paths, yet the checkpoint's own screen content requires **"Schedule active in the left nav"**, and converting `/trainer` to a redirect would otherwise have left a **"Dashboard" nav item that redirects away from the dashboard** — a misleading control this checkpoint would have created. The trainer rail's first item is therefore now **Schedule → `/trainer/schedule`**, and the shell's trainer `home` follows it. **The Management and Parent rails were not touched.** (2) `components/ui/icon.tsx` gained one **additive** `calendar` glyph for that item; no existing icon was altered. **Recorded but deliberately NOT fixed, because they are outside this screen's surface:** the shell nav/identity/footer contrast failures, `brand-mark.tsx`'s hardcoded `/trainer` href, the absent skip-to-content link, `trainer-dashboard.tsx`'s undefined `bg-warning-800` token, and the unlayered `app/globals.css` rules recorded at F-01b.
- **Next action:** Operator review of F4. **F5 (06 Trainer Student Roster)** is the next screen in flow order and was **not** started — one screen, one checkpoint, one stop. **F12 and F7–F9 remain blocked by F6**, which is itself blocked on Backend V2 or a coordinated operator authorization. The canonical child routes of screen `05` — `/trainer/schedule/[sessionId]/student-roster` and below — remain **unmoved**; their pinned paths are still the implemented ones and their replacement is F5/F16 work, not this checkpoint's.

### 2026-08-06 — FRONTEND RECONSTRUCTION F5 (06 Trainer Student Roster)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F5` — screen `06` Trainer Student Roster, node `487:9`, pinned route `/trainer/sessions/[sessionId]/roster`, physical-test flow order 3. Executed as operator checkpoint **F-05**.
- **Starting commit:** `468ac56a87a162a7b50d19c49986de055b852a5a` (F4)
- **Ending commit:** The commit created by `feat(frontend): reconstruct trainer student roster`
- **Status:** Ready for review — **screen `06` proposed visually accepted**
- **Reference integrity:** `06-trainer-student-roster/reference.png` SHA-256 `78e4b618…13659a` verified **before** any work began and **unchanged after** — 1440 × 1120, 119,195 bytes, byte-identical to the tracker checksum table and to `screen.md` §3.
- **Scope completed:** Screen `06` was rebuilt from the frozen frame: breadcrumb (`Schedule / <Class Grade> · <Class Module>`) and the **"Back to Schedule"** control, both pointing at **`/trainer/schedule`** — the canonical Trainer entry route F-04 created under operator ruling R-B1; the **"Student Roster"** page title; the dark-accent **Class Session banner** carrying the session identity and the **assessment-progress readout with its bar**; the three-region **session strip** (this session · carried-over focus · lesson plan); and the **four-column learner card grid** with its **Filter** and **Sort** controls, present/absent states and per-status actions. Loading, empty-roster, no-filter-match, failed and unavailable states are all built. **No route, DTO, port method, permission or fixture was added or changed** — `lib/frontend/contracts/`, `lib/frontend/fixtures/` and `lib/frontend/physical-test-port.ts` are untouched, and the **route census is unchanged at 17 application routes plus `/_not-found`**.
- **NO ROUTE WAS MOVED.** The screen stays on its **pinned** `/trainer/sessions/[sessionId]/roster` path. Its canonical route `/trainer/schedule/[sessionId]/student-roster` and the `Replace after integration` treatment recorded for it (inventory §7.2, `screen.md` §1) remain **unexecuted** and need their own authorization — `screen.md` §6 forbids renaming the route family before that separately-authorized migration checkpoint.
- **THE TWO GOVERNANCE RULES A STATIC FRAME CANNOT DEMONSTRATE — this is the substance of F-05.** **(1) Absence exposes nothing.** `resolveAction` gates on **attendance before it ever reads a report status**, so an absent learner's card offers **no assessment path and no report path — no link, no route, and deliberately no lifecycle status pill either**, because absence must never create or expose a fabricated assessment or report (A-018, `CLAUDE.md` §6). Attendance itself arrives **resolved on the governed projection**: nothing here computes, infers, defaults or toggles it, and no enrolment eligibility is computed client-side (`screen.md` §6 "Prohibited invention"). **(2) The per-student action gates on that student's ACTUAL report status.** There is deliberately **no one generic handler shared across the cards**: `resolveAction` is an exhaustive switch over that learner's own `reportState` with a `never`-typed exhaustiveness check, and every branch resolves its own destination — `no_report`/`incomplete` → **Assess**; `observation_saved`/`drafting` → **Continue**; `draft_ready`/`needs_edit` → **Review**; `trainer_approved`/`approved`/`submitted` → **View report**. A state naming no reachable report renders an **inert control with a non-disclosing reason**, never a guessed destination. Both rules are asserted in the browser against a production build, not merely coded.
- **CONTINUITY NOW THREADS THROUGH THE LIVE ROSTER (persona §3.8, Phase 1 exit condition (c)).** `previousSessionFocus` is carried **on every present learner's card** *and* summarised across the session in the strip's **"Focus carried over from the previous session"** region, so the trainer meets the previous session's follow-up while teaching rather than only in the database.
- **TOKEN CONVERGENCE — the verifier finding is discharged.** The `bg-slate-100` / `text-slate-500` disabled treatment and the `bg-indigo-100` / `text-indigo-700` pending-review treatment are gone, along with this screen's `navy-*`, `text-lg`, `text-sm` and `text-xs` legacy utilities. The surface is now entirely F1 project tokens, and the smoke suite asserts in the live DOM that **no element under `main` carries a `slate` / `gray` / `zinc` / `indigo` palette class**, so a regression fails the suite rather than passing review.
- **A REAL WCAG 2.2 AA DEFECT WAS FOUND AND FIXED INSIDE THIS CHECKPOINT — exactly the one F-01b predicted.** `app/globals.css` declares `h1, h2, h3, h4 { color: #1b2b4b }` **unlayered**, which outranks every rule in `@layer utilities`; `text-white` on the dark Class Session banner heading was therefore generated, matched, and **silently lost the cascade**, rendering near-black heading text on the accent surface at a measured **1.11:1**. `globals.css` is outside this checkpoint's owned paths, so the fix is **local and does not touch it** — the colour is carried on an inner `span` the unlayered selector cannot match, with the same treatment applied to the strip's region labels and the absent learner's name, whose intended muted colour was being overwritten the same way. **No `!important`, no unowned file edited.**
- **Eight frame-versus-governance deviations recorded, not silently resolved (A-045).** **D1 "CLASS IN SESSION"** asserts a **session lifecycle state**; that enum is deferred and unratified and A-026 says plainly "do not invent a placeholder enum", so the eyebrow names the governed entity — **"CLASS SESSION"** — and the live dot goes with it (F-04's D2 adjudication). **D2** the frame's **lesson number, lesson title and room** exist on no governed field and are **omitted rather than fabricated**. **D3** the **"KEY FOCUS"** chips are lesson-plan tags with no backing; the region is kept and filled from the only governed focus data on this screen — the carried-over previous-session focus — and **labelled for what it actually is**. **D4** the **"SLIDES"** attachment chips are omitted, and **"View lesson plan"** keeps the frame's label rendered **disabled with a visible, programmatically associated reason** (the F-04 D1 / F-11 treatment). **D5** the frame's **"Trainer: <name>"** needs an assignment-name field the projection does not carry. **D6** the frame draws **eight synthetic learner cards**; **Figma mock data is never ported** (`GLOBAL_UI_RULES` §8), so the grid renders exactly the four entries the governed projection returns, in the frame's four-column composition. **D7** the frame highlights **Schedule** in the left rail; that rail is `components/layout/portal-shell.tsx`, **outside the owned paths**, and its Schedule item matches `/trainer/schedule` exactly while this screen still sits on the pinned `/trainer/sessions/...` path — the highlight follows the **separately-authorized route move**, not this checkpoint, and the relationship is instead carried by the breadcrumb and back control the frame also draws. **D8** the shared `Avatar` `muted` variant measures **2.004:1**; `components/ui/avatar.tsx` is unowned, so the frame's inert initials treatment is reproduced locally at the AA floor — hue preserved, luminance moved.
- **Five dependencies recorded, none invented:** (1) **lesson plan / lesson number / lesson title**, (2) **session materials or slide attachments**, (3) **session room or location**, (4) **assigned-trainer name** — (3) and (4) are the same gaps F-04 recorded for screen `05` — and (5) a **Trainer-reachable attendance toggle**. A-018 gives the trainer the right to mark an individual learner Absent and the frame draws attendance state but no control; **no attendance mutation exists on `PhysicalTestPort`**, so **no toggle was built, faked or stubbed** and the roster renders the server-resolved state only.
- **Not rating-bearing:** screen `06` surfaces no competency-rating vocabulary (`screen.md` §8), so F5 carries **no F6 dependency**. The rating union in `lib/frontend/contracts/physical-test.ts` still declares the superseded `emerging`/`developing`/`secure`/`advanced` and was **not touched** — the correct pre-V3 state, which belongs to F6. The **Class Grade** vocabulary this screen renders is a **different, unchanged vocabulary** (A-054): `Beginner` / `Intermediate` / `Advanced` render verbatim and mark themselves `data-vocabulary="class-grade"` so a rating-token guard classifies them by **actual context**, and **no keyword replacement** over `advanced`/`secure`/`emerging`/`beginning`/`mastering`/`mastered` was performed anywhere.
- **Accessibility:** heading outline `h1 → h2 → h3` with no skip; the breadcrumb is a `nav`/`ol` with an `aria-hidden` separator; Filter and Sort are real `select`s with programmatically associated labels; the progress bar is `aria-hidden` because the identical fact is stated in text ("2 of 3 present learners assessed"); attendance is carried by an icon **and** the words Present / Absent, never by colour alone; every action carries an `sr-only` learner-name suffix so its accessible name is unique across the grid; empty and no-match states carry `role="status"`; every interactive target is ≥ 44 px. Contrast was **measured from live computed styles in the production build** across **60** text nodes: **53 pass and every owned-surface node passes.** The 5 residual failures are all `components/layout/portal-shell.tsx` at 2.828:1 (identity sub-label and fixture footer) — **pre-existing and outside the owned surface**, the same adjudication F-04 recorded; the 2 remaining are **`disabled`** controls at 2.043:1, which **SC 1.4.3 expressly exempts** as inactive user-interface components. **No horizontal page scroll at 1440, 1024 or 480 px** — `documentElement.scrollWidth` measured 1440 / 1024 / 480, equal at all three.
- **Files changed:** `features/trainer/trainer-roster.tsx`; `tests/frontend/trainer-browser-smoke.mjs` (**assertions only** — additive, no existing assertion weakened or removed); this frontend workstream log. **No route file, no shared component, no DTO, no port, no fixture, no `package.json` and no `package-lock.json` was touched.**
- **Tests and validation:** `06-trainer-student-roster/reference.png` SHA-256 verified **before and after — unchanged**; `node_modules/.bin/tsc --noEmit` — exit 0; `node_modules/.bin/eslint .` — exit 0; `npm run build` — exit 0, **route census unchanged at 17 application routes** plus `/_not-found`, none added, removed or renamed; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0, now **9 check groups** including the new screen `06` group; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0; `git diff --check` — exit 0. **All three browser suites were run against a PRODUCTION build (`next start`), never a cold dev server**, and the server was restarted after the final rebuild. **Zero uncaught browser-console/runtime errors** in every run. **No dependency, no test runner, no `package.json` or `package-lock.json` change of any kind** (R-B7). Six diagnostic renders — the reference **1440 × 1120**, **1024 × 900**, **480 × 900**, the absent-only filter, the empty-roster projection and the non-disclosing unavailable state — plus the full contrast and overflow measurements were written **outside Git** to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F-05\`. Synthetic fixture data only.
- **One process hazard hit and recorded so it is not re-learned:** an **orphaned `next start` from an earlier session was still holding port 3000** and serving stale chunks, which failed the first suite run at the login surface. It was terminated and every result above comes from a server started on the final build — the same class of hazard F-04 recorded for rebuilding underneath a running server.
- **Unresolved blockers:** none. The five dependencies above are recorded for the operator, not blocking.
- **Contract deviations requested:** none. **No owned-path deviation was needed** — every change is inside `features/trainer/trainer-roster.tsx` and the trainer smoke suite's assertions. **Recorded but deliberately NOT fixed, because they are outside this screen's owned surface:** the portal-shell nav/identity/footer contrast failures and the Schedule nav highlight (D7); `brand-mark.tsx`'s hardcoded `/trainer` href; the absent skip-to-content link; `trainer-dashboard.tsx`'s undefined `bg-warning-800` token; and the remaining unlayered `app/globals.css` rules recorded at F-01b — this checkpoint worked **around** the `h1–h4` rule locally rather than editing an unowned file.
- **Next action:** Operator review of F5. **F6 (the Amendment 006 frontend vocabulary checkpoint) remains blocked** on Backend V2 or a coordinated operator authorization, and **F7–F9 and F12 remain blocked behind it**. Screen `07` and beyond were **not** started — one screen, one checkpoint, one stop. The canonical route move for screen `06` — `/trainer/schedule/[sessionId]/student-roster` — remains **unexecuted** and is F16 / route-migration work, not this checkpoint's.

### 2026-08-06 — FRONTEND RECONSTRUCTION F6 (Amendment 006 competency-vocabulary adoption)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F6` — the Amendment 006 (**A-049 … A-055**) frontend vocabulary adoption, the reconciliation plan's **V3**. Executed as operator checkpoint **F-06** under operator ruling **R-B13**. **This is NOT a visual screen checkpoint.** No screen was reconstructed, no route was created, moved, renamed or redirected, no component composition changed, and **no screen is claimed visually accepted**.
- **Starting commit:** `84f7d8028729afa06fa36094d6dcb8e4810f8745` (F5)
- **Ending commit:** The commit created by `feat(frontend): adopt the ratified competency vocabulary (Amendment 006 A-049)`
- **Status:** Ready for review
- **Reference integrity:** all **12** frozen `reference.png` files SHA-256 hashed **before any work began** and re-hashed **after** — **12/12 byte-identical, 0 mismatch**. No reference asset was opened for writing, moved, renamed or re-encoded. F-06 touches no frozen frame because it implements a ratified vocabulary, not a frame.
- **Scope completed — the four things A-049 … A-051 actually require, and nothing else.** **(1) Display labels and storage values.** `Beginning · Developing · Mastering · Mastered`, stored `beginning · developing · mastering · mastered`. **(2) Arity, order and universality.** Four levels, ordered **low to high**, applied to all nine dimensions, **all nine mandatory** (A-017); **no fifth level**; `developing` is **unchanged in both value and ordinal position**, which is exactly why the authorized backend migration renames three labels and not four. **(3) Anchors.** All four behavioural anchors re-keyed **positionally** and now **byte-identical to the backend**. **(4) Polarity.** `beginning → needs_support`, `developing → developing`, `mastering → positive`, `mastered → positive` — declared explicitly in the frontend for the first time, agreeing exactly with the backend. Fixture rating sets, the assessment chips, the anchor help text, the missing-rating error sentence, the Trainer review snapshots and the grounding-rejection prose all move to the ratified vocabulary.
- **THE ANCHORS AND POLARITY ARE PROVEN AGAINST THE BACKEND, NOT ASSERTED.** The backend constants were read **READ-ONLY** from `worktrees/backend-48h/server/modules/framework/dimensions.ts` (Backend V2 — `e5a66d7`, `103f433`, `ec5be57`). **Nothing was written to, staged in, or committed in the backend worktree.** The comparison is recorded in `_checkpoint-evidence/F-06/backend-frontend-vocabulary-parity.json`: rating union `["beginning","developing","mastering","mastered"]` on **both** sides — `agreeExactly: true`; all four anchors compared by **byte length and SHA-256 pair** — `90/90`, `100/100`, `105/105`, `129/129` bytes, four matching digests, `anchorsAllByteIdentical: true`; polarity tables compared key by key — `agreeExactly: true`, `masteringIsPositive: true`. Backend V2's **database-dependent** gates remain BLOCKED on the operator-only fixture-credential action (**R-27**); that does not affect these constants, which are committed source.
- **ONE REAL DEFECT WAS FOUND BY THAT COMPARISON AND FIXED — this is the substance of the anchor requirement.** The frontend's **level-4** anchor was a **paraphrase** of the backend's: *"Exceeds the expected level through confident, natural, independent application across different contexts."* against v3 §3.3's *"Exceeds the expected level: strong confidence, natural expression, independent application, consistent across different contexts."* A-050 makes **v3 §3.3's wording authoritative** and requires the backend and frontend copies to be **byte-identical**, and states that where a restatement differs, v3 §3.3 governs and the restatement is a paraphrase. The divergence was therefore a **defect, not a variant** — it would have failed V4. It is corrected and the byte-identity is now proven by digest. The other three anchors were already correct and were re-keyed without a character changing.
- **`MASTERING` IS `POSITIVE`, AND IT IS RECORDED AS A RULING.** `POLARITY_BANDS` is declared in `lib/frontend/fixtures/dimensions.ts` with the reasoning attached in the file: polarity derives from the **ratified behavioural anchor** — independent, consistent demonstration across most classroom activities — **never from the progressive grammatical form of the label**. Demoting it because the word ends in *-ing* would silently narrow what the AI may describe as a strength in **every** report, which A-051 expressly does not permit. **Only `beginning` carries `needs_support`.** The chip treatment follows the band, not the grammar: the two positive levels are distinguished by **hue** (teal / green), never by one of them reading negative.
- **CLASS GRADE IS PROVEN BYTE-UNCHANGED (A-054).** `readonly classGrade: "Beginner" | "Intermediate" | "Advanced";` is character-for-character what it was at the starting commit in both the contract and the fixture, and the three fixture sessions still read `Beginner`, `Intermediate` and `Advanced` — verified by diffing every `classGrade`-bearing line against `HEAD`: **only line numbers shifted; no character changed**. **`Advanced` is no longer a competency rating and IS still a Class Grade**, and every occurrence in this checkpoint was classified by **actual context**.
- **NO GLOBAL KEYWORD REPLACEMENT WAS PERFORMED — this is stated precisely because the prohibition is absolute.** No search-and-replace was run over `advanced`, `secure`, `emerging`, `beginning`, `mastering` or `mastered` anywhere in the repository or its documentation. Every occurrence was changed deliberately after establishing what it is. Three worked examples: the fixture identifier `ADVANCED_MIXED_RATINGS` **was** renamed to `MASTERED_MIXED_RATINGS` because its values are **competency ratings**; `classGrade: "Advanced"` three lines away **was not touched** because it is a **Class Grade**; and `trainer-schedule.tsx`'s `Advanced: "success"` class-grade tone map and `trainer-roster.tsx`'s class-grade comment — both outside the owned paths and both **Class Grade** — were likewise left alone.
- **THE ASSERTIONS ARE NOT SWAPPED FROM ONE PIN TO ANOTHER — A-052's highest-risk failure mode is addressed head-on.** A-052 records that an assertion left pinned to labels that no longer exist *"continues to pass while checking for values that no longer exist"*, a silent degradation to a false negative. The parent-surface guard's token list therefore now carries **both** vocabularies: the four ratified labels **and** the superseded `emerging`/`secure`/`advanced`, so it checks the live vocabulary **and** keeps the old one as a regression guard. It remains an **isolated-raw-label** check — a leaf element whose entire text is one token, or an element carrying a rating-bearing data attribute — and is **never** the bare-word prose regex `\b(beginning|developing|mastering|mastered)\b` that A-052 **prohibits**, because "at the beginning of the session" and "has mastered maintaining eye contact" are legal parent-facing English. Class Grade is skipped by `data-vocabulary="class-grade"`, so occurrences are classified by context, never by keyword.
- **ALL FOUR RATING STATES ARE COVERED IN THE BROWSER AND IN THE DOM, not merely in the source.** The trainer suite now asserts, against a production build: every one of the **nine** dimensions offers exactly **four** chips; their labels are `Beginning · Developing · Mastering · Mastered` **in that low-to-high order**; each chip's `data-rating-level` is the matching **storage value**, so a relabel that left the stored value behind would fail rather than read correctly on screen; **no superseded competency label renders as a control**; selecting each of the four levels surfaces **that level's ratified anchor verbatim**; the nine-dimension walkthrough is asserted to exercise **all four** states (`new Set(...).size === 4`); and the Trainer review surface renders **nine** snapshots, every one inside the ratified vocabulary, with **each of the four states proven present**.
- **WCAG 2.2 AA RE-VERIFIED FOR ALL FOUR RATING STATES, MEASURED IN THE RENDERED PRODUCTION DOM.** Eight measurements — each of the four chips in **idle** and in **selected** state: `Beginning` 7.638 / 8.225, `Developing` 8.735 / 8.131, `Mastering` 9.023 / 8.339, `Mastered` 8.647 / 8.228. **Minimum 7.638:1; every measurement ≥ 4.5:1**, so SC 1.4.3 passes for the label text of all four new chips with substantial margin. The measurement is repeated inside `trainer-browser-smoke.mjs`, so a regression fails the suite rather than passing review.
- **A MEASUREMENT DEFECT WAS CAUGHT INSIDE THIS CHECKPOINT AND IS RECORDED SO IT IS NOT RE-LEARNED.** The first contrast harness scraped numbers out of `getComputedStyle().color` with `[\d.]+`. **Tailwind v4 resolves these tokens to `oklab()` / `lab()`, whose components can be NEGATIVE** — `lab(29.506 -21.4706 -3.59886)` — so the scrape silently dropped every minus sign and produced `NaN`, which CDP returns as an **unserializable value**, surfacing as `undefined` rather than as an error. Had the arithmetic degraded slightly differently it would have reported a **plausible but wrong ratio and passed**. The harness now rasterises the computed colour through a **1 × 1 canvas** and reads the **sRGB bytes the browser actually paints**, which is the thing SC 1.4.3 is about, and the assertion additionally requires the result to be a **finite number** before comparing it to 4.5.
- **Governance boundaries held.** **No route was created, moved, renamed or redirected** — route census **unchanged at 17** application routes plus `/_not-found`. **No port method, no permission, no DTO beyond the rating union, no lifecycle rule, no privacy boundary and no screen composition changed.** No dependency, no test runner, no `package.json` and no `package-lock.json` change of any kind (**R-B7**); `npm audit fix` was not run. The frozen demo at `SDS Project Sprint 2` was not touched. `governance-source/` was **not read** — the repository copies govern (**A-055**). No enum, migration, generated type or backend artefact was authored: the **A-053** migration belongs to the backend and is **not** this checkpoint's, and no generated database type was hand-edited.
- **Files changed:** `lib/frontend/contracts/physical-test.ts` (the rating union on lines 1–8 only — every other declaration, including `classGrade`, byte-unchanged); `lib/frontend/fixtures/dimensions.ts`; `lib/frontend/fixtures/physical-test-fixture.ts`; `features/trainer/trainer-assessment.tsx`; `features/trainer/trainer-report-review.tsx`; `tests/frontend/trainer-browser-smoke.mjs` and `tests/frontend/three-role-browser-smoke.mjs` (**assertions only** — additive; **no existing assertion was weakened or removed**); this frontend workstream log. **No route file, no shared component, no unowned feature file and no `.assertions.ts` needed changing** — `fixture-contract.assertions.ts` already pins the rating count structurally (`const governedRatingCount: 4 = RATING_LEVELS.length`) and continues to hold against the new union, which is the correct outcome for a same-arity relabel.
- **Tests and validation:** 12/12 `reference.png` SHA-256 verified **before and after — unchanged**; `node_modules/.bin/tsc --noEmit` — exit 0; `node_modules/.bin/eslint .` — exit 0; `npm run build` — exit 0, **route census 17 application routes plus `/_not-found`, unchanged, none added, removed or renamed**; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0; `git diff --check` — exit 0. **All three browser suites ran against a PRODUCTION build (`next start`), never a cold dev server**, and the server was started on the final build. **Zero uncaught browser-console/runtime errors** in every run. Diagnostic renders — one per rating state plus the nine-snapshot review surface — and the WCAG and backend-parity measurement files were written **outside Git** to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F-06\`. Synthetic fixture data only; no credential was requested, accepted, printed or persisted.
- **Unresolved blockers:** none for this checkpoint. Backend V2's **database-dependent** gates stay blocked on the operator-only fixture-credential action (**R-27**) — an operator item, not a frontend one, and it does not affect the committed constants matched here.
- **Contract deviations requested:** none. **No owned-path deviation was needed.** **Recorded but deliberately NOT fixed, because they are outside the owned paths:** `portal-shell.tsx`'s `text-ink-muted` nav links at ~3.08:1; `brand-mark.tsx:86`'s hardcoded `href="/trainer"` inside the Parent and Management shells; the absent skip-to-content bypass link; `trainer-dashboard.tsx:72`'s undefined `bg-warning-800` token; and the unlayered `*`, `body`, `h1`–`h4`, `:focus-visible`, `::selection`, `.card`, `.panel` and `.form-field` rules in `app/globals.css`.
- **Next action:** Operator review of F6. The vocabulary gate that blocked **F7, F8, F9 and F12** is **discharged**; each still requires its own authorization, and **F8's five other recorded blockers stand unchanged** (term-report framing, the evidence upload zone under an UNRESOLVED uploader, the Report Details field inventory, and "Confirm & Submit" as a trainer control with no Quality Checklist). No screen was started — one checkpoint, one stop.

### 2026-08-06 — FRONTEND RECONSTRUCTION F7 (07 Trainer Grade Student)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F7` — screen `07` Trainer Grade Student, node `784:679`, pinned route `/trainer/sessions/[sessionId]/students/[studentId]/assess`, physical-test flow order 4. Executed as operator checkpoint **F-07**. **This is the assessment instrument** — the most governance-dense surface in the 48-hour slice.
- **Starting commit:** `5dcbeeb6c45e97506cf2404e37df4e0d00b9dff0` (F6)
- **Ending commit:** The commit created by `feat(frontend): reconstruct trainer grade student`
- **Status:** Ready for review — **screen `07` proposed visually accepted**
- **Reference integrity:** `07-trainer-grade-student/reference.png` SHA-256 `1df95a5b…13199d` verified **before** any work began and **unchanged after** — 1650 × 1200, 131,418 bytes, byte-identical to the tracker checksum table and to `screen.md` §3.
- **Scope completed:** Screen `07` was rebuilt from the frozen frame: the **"Grade Student"** page title with the **"Back to Student Roster"** control; the **REVIEW & APPROVE** counter rail (Not started / In progress / Pending approval / Approved) with the Class Session's learner list; the learner **identity card**; the single **"Assessment Rubric"** card carrying **all nine** four-segment rows with the frame's *"Select a level for each speaking skill"* microcopy; the **"Observation Notes"** card; and the bottom-right **"Save & Generate"** action. Validation, loading, failure/retry, returned-correction, disabled/read-only-after-save, empty-rail and non-disclosing-unavailable states are all built. **No route, DTO, port method, permission or fixture was added or changed** — `lib/frontend/contracts/`, `lib/frontend/fixtures/` and `lib/frontend/physical-test-port.ts` are **byte-unchanged**, and the **route census is unchanged at 17 application routes plus `/_not-found`**.
- **NO ROUTE WAS MOVED.** The screen stays on its **pinned** `/trainer/sessions/[sessionId]/students/[studentId]/assess` path. Its canonical route `/trainer/schedule/[sessionId]/student-roster/[studentId]/grade-student` and the `Replace after integration; pinned path preserved as a redirect` treatment recorded for it (inventory §7.2, `screen.md` §1) remain **unexecuted** and need their own authorization.
- **A-017 IS THE POINT OF THIS SCREEN, AND IT IS HELD EXACTLY.** There is **one capture mode: the full nine.** No four-dimension path, no mode toggle, no `observations.mode`, no `mode` prop, no `mode` branch and no Quick/Full control exists anywhere on the owned surface — and the browser suite now asserts **both** that no Quick/Full copy renders **and** that no control is *named* Quick or Full, so a reintroduction fails the suite rather than passing review. The nine render from `DIMENSION_CODES`, which **is** the ratified order, and that order is asserted element-by-element in the rendered DOM.
- **EVERY DIMENSION SURFACES ITS BEHAVIOURAL ANCHOR — AND THE FRAME DRAWS NONE.** `CLAUDE.md` §5 requires the anchor alongside the rating; the frozen frame shows a bare four-segment track with no anchor anywhere. Governance wins: an anchor line is **added** beneath every one of the nine rows, rendered **verbatim** (A-050 byte-identity, proven by digest at F-06 and unchanged here), and **each chip's accessible name carries that level's anchor**, so the behavioural meaning reaches assistive technology **before** the choice is made rather than only after.
- **THE SHARED FOLLOW-UP FIELD IS LOADED, NOT BLANKED.** "Follow-up for Next Session" and Review & Approve's "Coach Notes (Internal Only)" are the **same `observations.follow_up_notes` column** (`CLAUDE.md` §6). The frame draws no such field at all; governance wins, the field is **added**, its help text names the relationship, and the browser suite proves the **loaded value is non-empty** against a learner that carries one (Learner Cedar's returned report) rather than against a fresh observation where empty would be legitimate and the assertion vacuous.
- **THE CLIENT CHECK IS NAMED AS A CONVENIENCE, IN THE UI ITSELF (ADR-3).** The save-gate copy now reads *"Completion is re-validated by the governed save against all nine dimensions — the check on this form is a convenience only."* Nothing on the screen implies the form's all-nine check is authoritative.
- **Eight frame-versus-governance deviations recorded, not silently resolved (A-045).** **D1 ROW ORDER** — the frame **interleaves** the two governed groups (Body · Eye contact · Emotion · Speech · Tonality · Vocal projection · Emotional expression · Sentence flow · Audience awareness); the ratified order is the four Competency dimensions **then** the five Speech Linguistics dimensions, so the rows render in ratified order with the two groups captioned. **D2** the frame draws **no behavioural anchor**; one is added per row (above). **D3** the frame draws **no Follow-up field**; one is added (above). **D4 "Junior · Student ID 2025-113 · Public Speaking"** — "Junior" is **not** a governed Class Grade (the only values are Beginner / Intermediate / Advanced, A-016/A-054), no governed learner-facing student number exists, and rendering the internal `studentId` as a "Student ID" would manufacture a user-facing identifier; **both omitted**, and the governed Class Grade, Class Module and session date are shown instead. **D5 SELECTED-CHIP FILLS** — the frame's saturated fills with white label text measure **3.70 / 2.03 / 2.34 / 2.51 : 1** and **all four fail SC 1.4.3**; the fill moves to the same hue's deeper F1 ramp step, hue preserved and luminance moved. **D6** the frame draws **eight** synthetic learners in the rail; Figma mock data is never ported, so the rail renders exactly the four entries the governed projection returns. **D7** the frame gives **every** rail learner a lifecycle status; an **absent** learner gets **none**, no path, and no place in any counter (A-018). **D8** the frame's rail highlight and its "Students"/"Reports" nav items belong to `components/layout/portal-shell.tsx`, **outside the owned paths** — the same adjudication F-05 recorded as its D7.
- **Three dependencies recorded, none invented:** (1) a **learner-facing student number**, (2) a **learner year/stage label** ("Junior"), and (3) a **programme/subject label distinct from the Class Module** ("Public Speaking"). All three appear in the frame, none exists on any governed DTO, and none was fabricated or mapped onto a vocabulary it does not belong to.
- **A REAL MEASUREMENT DEFECT WAS FOUND AND FIXED INSIDE THIS CHECKPOINT — and it invalidates a number F-06 reported.** F-06's **selected**-chip contrast readings (8.225 / 8.131 / 8.339 / 8.228) were taken **mid-transition** and were not measuring the settled selected state at all. `getComputedStyle` returns the **currently interpolated** value while a CSS transition runs; every rating chip carries `transition`, and the reading fired as soon as the anchor text repainted — well inside the 150 ms window. On this build that produced **9.502:1 for all four states**, which is the **idle** pair (`#33405c` on `#f4f5f9`) reported for a chip whose `className` was already the selected one: a **plausible, passing and wrong** number. The harness now **emulates `prefers-reduced-motion: reduce`** (collapsing every transition to 0.01 ms through the reduced-motion block already in `app/globals.css`) **and** waits for the selected chip's computed background to be non-transparent before reading. The settled values are **6.251 / 5.536 / 5.929 / 6.381 : 1** — all four still clear 4.5:1, so **F-06's conclusion survives but the measurement that supported it did not**. This is the same class of defect F-06 itself recorded for the `oklab()` sign scrape: a harness that degrades to a plausible number instead of to an error.
- **WCAG 2.2 AA, MEASURED IN THE RENDERED PRODUCTION DOM.** All four rating states are now measured in **both** treatments — **idle 9.502:1** each, **selected 6.251 / 5.536 / 5.929 / 6.381**. Contrast was measured across **135** text nodes: **130 pass and every owned-surface node passes**; the 5 residual failures are all `components/layout/portal-shell.tsx` at 2.828:1 (identity sub-label and fixture footer), pre-existing and outside the owned surface — the same adjudication F-04 and F-05 recorded. A **second** AA defect was found and fixed locally: the shared `Avatar` `muted` variant measures **2.004:1**, and since `components/ui/avatar.tsx` is unowned, the absent learner's inert initials are reproduced locally at the AA floor (5.02:1) — the F-05 D8 treatment, and still an open defect in a shared component. **No horizontal page scroll** — `documentElement.scrollWidth` measured **1650 / 1024 / 480** at those three widths, equal at all three.
- **Accessibility beyond contrast:** heading outline `h1 → h2 → h3` with no skip; each dimension is a `fieldset` whose `legend` names the dimension, its focus and "(required)"; **all 36 rating controls carry a real, distinct accessible name** — asserted as 36 distinct names in the DOM, because without it they would expose only four names repeated nine times; **selection is never carried by colour alone** (`aria-pressed`, a check glyph, ", selected" in the accessible name, a 2 px border that survives forced-colours mode, and the visible anchor line naming the chosen level in words); the validation banner is `role="alert"`, states how many dimensions are missing, and **focuses and scrolls the first missing fieldset**, which is `aria-invalid` with its error wired by `aria-describedby`; both textareas have real labels; every target is ≥ 44 px; the instrument is **keyboard operable end to end**.
- **No shared rating primitive was created, deliberately.** The four-segment control has exactly **one** consumer in this build — the Trainer review surface renders read-only snapshots, not a selector — so promoting it to `components/ui/**` would create an abstraction with a single caller and put governed rating presentation in a file no rating-bearing checkpoint owns. It stays local to `features/trainer/trainer-assessment.tsx`.
- **Vocabulary:** the ratified A-049 vocabulary was already live on this branch (F-06, `5dcbeeb`, verified before any code was written). The nine rows render **Beginning / Developing / Mastering / Mastered** with storage values `beginning` / `developing` / `mastering` / `mastered`, low to high, on all nine dimensions. **No anchor was reworded, re-wrapped or paraphrased. No enum label was renamed. No global keyword replacement was performed** over `advanced`, `secure`, `emerging`, `beginning`, `mastering` or `mastered`. The **Class Grade** the identity card renders (`Beginner`) is a **different, unchanged** vocabulary (A-054) and marks itself `data-vocabulary="class-grade"` so a rating-token guard classifies it by **actual context**.
- **Files changed:** `features/trainer/trainer-assessment.tsx`; `tests/frontend/trainer-browser-smoke.mjs` (**assertions and the contrast harness only** — additive; **no existing assertion was weakened or removed**, and the three renamed button-label lookups follow the frame's microcopy `Save & Generate`); this frontend workstream log. **No route file, no shared component, no DTO, no port, no fixture, no `package.json` and no `package-lock.json` was touched.**
- **Tests and validation:** `07-trainer-grade-student/reference.png` SHA-256 verified **before and after — unchanged**; `node_modules/.bin/tsc --noEmit` — exit 0; `node_modules/.bin/eslint .` — exit 0; `npm run build` — exit 0, **route census unchanged at 17 application routes** plus `/_not-found`, none added, removed or renamed; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0, now **10 check groups** including the new screen `07` group; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0; `git diff --check` — exit 0. **All three browser suites ran against a PRODUCTION build (`next build` + `next start`), never a cold dev server**, and the server was **restarted on the final build** after the last rebuild — the stale-server hazard F-04 and F-05 recorded was hit again here and is recorded again so it is not re-learned. **Zero uncaught browser-console/runtime errors** in every run. **No dependency, no test runner, no `package.json` or `package-lock.json` change of any kind** (R-B7); `npm audit fix` was not run. `governance-source/` was **not read** (A-055). The frozen demo at `SDS Project Sprint 2` was not touched. Seven diagnostic renders — the reference **1650 × 1200** unrated, the validation state, all nine rated exercising all four levels, **1024 × 900**, **480 × 900**, the returned-correction state carrying its loaded Follow-up value, and the non-disclosing unavailable state for the absent learner — plus the full contrast and overflow measurements were written **outside Git** to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F-07\`. Synthetic fixture data only.
- **Unresolved blockers:** none. The three dependencies above are recorded for the operator, not blocking.
- **Contract deviations requested:** none. **No owned-path deviation was needed** — every change is inside `features/trainer/trainer-assessment.tsx` and the trainer smoke suite. **Recorded but deliberately NOT fixed, because they are outside this screen's owned paths:** `portal-shell.tsx`'s nav/identity/footer contrast (2.225:1 – 3.079:1); `brand-mark.tsx:86`'s hardcoded `href="/trainer"` in the Parent and Management shells; the absent skip-to-content bypass link; `trainer-dashboard.tsx:72`'s undefined `bg-warning-800` token; the unlayered `*`, `body`, `h1`–`h4`, `.card`, `.panel` and `.form-field` rules in `app/globals.css`; the `brand-700` white-on-pink primary action at 4.517:1; and the shared `Avatar` `muted` variant at 2.004:1 found here and worked around locally.
- **Next action:** Operator review of F7. **F8 remains BLOCKED on its four non-vocabulary blockers** (term-report framing, the evidence upload zone under an UNRESOLVED uploader, the Report Details field inventory, and "Confirm & Submit" as a trainer control with no Quality Checklist) — the F6 vocabulary gate that also blocked it is discharged, but those four stand. **F9 is unblocked on the vocabulary gate and still requires its own authorization.** Screen `08` and beyond were **not** started — one screen, one checkpoint, one stop. The canonical route move for screen `07` remains **unexecuted** and is F16 / route-migration work.

### 2026-08-06 — FRONTEND RECONSTRUCTION F9 (10 Trainer Student Report)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F9` — screen `10` Trainer Student Report, node `664:9`, pinned route `/trainer/reports/[reportId]/review` (+ `/edit`), physical-test flow order 6. Executed as operator checkpoint **F-09**. This is the **trainer approval gate** — the one surface where the two-stage workflow's first human decision is taken.
- **Starting commit:** `7d3f1bab8c1503044f788141ff9772d0036b8986` (F7)
- **Ending commit:** The commit created by `feat(frontend): reconstruct trainer student report`
- **Status:** Ready for review — **screen `10` proposed visually accepted** (the `/edit` sub-surface is **not** proposed accepted; no frame exists for it)
- **Reference integrity:** `10-trainer-student-report/reference.png` SHA-256 `e64291dc…4f730` verified **before** any work began and **unchanged after** — 1440 × 1351, 285,426 bytes, byte-identical to the tracker checksum table and to `screen.md` §3.
- **Scope completed:** Screen `10` was rebuilt from the frozen frame: the **"Student Report"** page title, the **`Reports / <Class Grade> / <learner>`** breadcrumb and the pink **"Back"** pill; the **official class-report card** — square learner avatar, pink `Class Report — <learner>` title, the session subtitle line, and the four icon-led, bullet-led content sections in the frame's composition; the **"Class Video Evidence"** region; and the **right rail** carrying **Report Details**, **Performance Summary** and the **"Report sent to management for approval"** state. Loading, returned-correction, saved-wording, saved-correction, checklist-error, approval-error, approved and non-disclosing-unavailable states are all built. **No route, DTO, port method, permission or fixture was added or changed** — `lib/frontend/contracts/`, `lib/frontend/fixtures/` and `lib/frontend/physical-test-port.ts` are **byte-unchanged**, and the **route census is unchanged at 17 application routes plus `/_not-found`**.
- **NO ROUTE WAS MOVED, AND OD-3 WAS NOT RESOLVED.** The screen stays on its **pinned** `/trainer/reports/[reportId]/review` path. Its canonical route `/trainer/reports/[reportId]` and the `add the canonical index route` treatment (inventory §7.2, `screen.md` §1) remain **unexecuted** and land at **F16**. **OD-3** — whether `/trainer/reports/[reportId]/edit` becomes a canonical sub-route of ID `10` or receives its own inventory ID once a frame exists (inventory §7.4) — is **RECORDED ONLY**, in the editor's own file header, in `implementation-notes.md` and here. It was **not** resolved, and no route treatment was executed to imply an answer.
- **THE GOVERNANCE THIS SCREEN EXISTS TO HOLD (A-033 … A-038), HELD EXACTLY.** The trainer action is **Approve**. It commits `draft_ready | needs_edit → trainer_approved`, freezes that version and **publishes nothing**. The control renders **visually disabled** until all three Quality Checklist items are checked, and the copy on the screen now states plainly that *"The server re-checks all three attestations for this exact version before it accepts the approval"* — so nothing implies the disabled button is itself the gate (`CLAUDE.md` §6, ADR-3). `expectedVersionId`, `expectedLockVersion` and `expectedContentHash` are all carried on the approve call. The confirmation modal and the success state **never** claim publication, parent visibility or a parent notification; the browser suite now asserts the **absence** of that class of claim with a pattern, not just the presence of the correct copy. A trainer edit **resets the checklist** and requires review and approval again; a **returned** report cannot be reapproved as-is and routes to a fresh correction or an **explicit reaffirmation naming the open correction request**. The **content hash is never rendered** (A-038) and the suite asserts it.
- **THE CARRY-OVER F-06 VERIFIER DEFECT IS FIXED AND RE-MEASURED — this was the one High finding carried into this checkpoint.** The nine per-dimension rating labels rendered `text-brand-600`: **one brand pink for all four states**, measuring **3.53:1** on white in the rendered production DOM and **failing SC 1.4.3**. They now carry the ordinal-keyed **`rating-N-on-soft` foreground on the matching `rating-N-soft` fill** — the F1 pairs deepened specifically to clear 4.5:1. **All four states re-measured in the rendered production DOM of a `next build` + `next start` server:** **Beginning 5.466:1 · Developing 5.134:1 · Mastering 5.311:1 · Mastered 5.733:1.** The measurement is now a **permanent assertion** in `trainer-browser-smoke.mjs`, keyed on `data-rating-level`, that requires **all four** states to be present and each to clear 4.5:1 — a fix proved only on the states that happen to be on screen is not proved. Colour is never the only carrier: every tile states its level in text.
- **Eight frame-versus-governance deviations recorded, not silently resolved (A-045).** **D1 "Official report"** — at `/review` the version on screen is the trainer's **working** version; only management's Approve & Submit makes a version canonical and parent-visible (A-033), so the frame's subtitle claim is a **false lifecycle claim** and is replaced by the governed status (*"Trainer working version"*); the suite asserts the string never renders. **D2 PANEL HEADINGS** — the frame's *Overview / Strengths / Areas for Development / Remarks* are **not** a rename of the governed four (spec §8: Today's Strength · Next Focus · Practice Suggestion · Session Takeaway): "Overview" and "Remarks" have **no governed counterpart**, and inventing a mapping would silently redefine what each stored field means to a parent. The frame's four-section composition, iconography, bullet treatment and order are reproduced; the **governed labels are kept**, and the suite asserts the four governed keys in order. **Recorded for operator adjudication.** **D3 "CLASS VIDEO EVIDENCE"** — evidence scope **and** the uploading role are **UNRESOLVED** (A-014), `PhysicalTestPort` exposes no evidence read or upload path, and `Evidence Pending` is deliberately not a stored status (A-036). The region is **kept with the frame's label and rendered INERT** with a visible, programmatically associated reason — the F-04 D1 / F-11 treatment — and **no uploader was invented**; the frame's *"Recorded 14 Mar 2035 · 3:24 · MP4"* metadata is **omitted rather than fabricated**. **D4 "Lesson" and "Term"** rows — no lesson-number, lesson-title or term field exists on any governed Trainer projection; omitted, with the omission **stated on screen** rather than left as a silent gap. **D5 "Overall Grade: Mastering"** — no governed overall or roll-up competency grade exists; a single headline rating would be a **derived assessment fact this frontend computed**, exactly the class of claim A-034/A-035 reserve to the governed assessment. Omitted; the nine governed snapshots are shown instead, and the suite asserts no such row exists. **D6 FOUR PERFORMANCE TILES** — the frame draws four of the nine (Speech, Tonality, Eye Contact, Audience Awareness); **no governed rule selects four**, and all nine are mandatory (A-017), so the frame's tile composition is reproduced across **all nine** governed snapshots rather than an invented subset. **D7 THE FRAME DRAWS ONLY THE APPROVED END STATE** — it shows no Quality Checklist, no Approve control, no returned-correction state and no internal Coach Notes. Those are governed functionality `screen.md` §6 requires preserved, so they are built and render for the states in which they apply; *"Report sent to management for approval"* is the `trainer_approved` state and is **correct and expected**. **D8** the frame's left rail (Dashboard / My Classes / Students / Reports / Schedule, Reports highlighted) belongs to `components/layout/portal-shell.tsx`, **outside the owned paths** — the same adjudication F-05 recorded as its D7; the relationship is carried by the breadcrumb and the Back control the frame also draws.
- **Four dependencies recorded, none invented:** (1) a **class-video-evidence read path plus a ratified uploader** (A-014, still UNRESOLVED — no upload path exists on `PhysicalTestPort`); (2) a **lesson number / lesson title** field; (3) a **term** field; (4) a **governed overall or roll-up competency grade**. All four appear in the frame, none exists on any governed DTO, and none was fabricated.
- **Vocabulary:** the ratified A-049 vocabulary was already live on this branch (F-06, `5dcbeeb`, verified before any code was written). The nine snapshots render **Beginning / Developing / Mastering / Mastered** from the single shared `RATING_DISPLAY_LABELS` table with storage values `beginning` / `developing` / `mastering` / `mastered`, each carried on `data-rating-level` so label and storage value are both assertable. **No anchor was reworded. No enum label was renamed. No global keyword replacement was performed** over `advanced`, `secure`, `emerging`, `beginning`, `mastering` or `mastered` (A-054). The **Class Grade** the breadcrumb, subtitle and Report Details render is a **different, unchanged** vocabulary and marks itself `data-vocabulary="class-grade"` so a rating-token guard classifies it by **actual context**.
- **The `/edit` sub-surface was restyled, not reconstructed, and is deliberately NOT proposed accepted.** `reference.png` covers node `664:9` — the review surface only. Rather than invent a frame (`GLOBAL_UI_RULES` §8 stop-and-ask), the editor was converged onto the **shared F1 foundation its sibling already uses** — tokens, card surfaces, field treatment, button primitives — with **no behaviour, field, label or governed call changed**. It also retired this file's last uses of the legacy `navy-*` aliases and the undefined-token-adjacent `bg-warning-800` / `hover:bg-amber-900` pairing.
- **Files changed:** `features/trainer/trainer-report-review.tsx`; `features/trainer/trainer-report-editor.tsx`; `tests/frontend/trainer-browser-smoke.mjs` (**assertions and a shared contrast core only** — additive; the one rewritten assertion, `article.length >= 5`, was **strengthened** into an exact ordered check of the four governed panel keys plus an explicit Coach Notes check, not weakened); this frontend workstream log. **No route file, no shared component, no DTO, no port, no fixture, no `package.json` and no `package-lock.json` was touched.**
- **Tests and validation:** `10-trainer-student-report/reference.png` SHA-256 verified **before and after — unchanged** (`e64291dc80a2af7378635a3daffe63952899768c41493e8a185da12119b4f730`); `node_modules/.bin/tsc --noEmit` — exit 0; `node_modules/.bin/eslint .` — exit 0; `npm run build` — exit 0, **route census unchanged at 17 application routes** plus `/_not-found`, none added, removed or renamed; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0, now **11 check groups** including the new screen `10` group; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0; compiled `fixture-contract.assertions.ts`, `fixture-lifecycle.assertions.ts`, `design-foundation.assertions.ts` and `auth-reference-fidelity.assertions.ts` — exit 0 each; `git diff --check` — exit 0. **All three browser suites ran against a PRODUCTION build (`next build` + `next start` on port 3411), never a cold dev server**, and the server was on the final build. **Zero uncaught browser-console/runtime errors** in every run. **No dependency, no test runner, no `package.json` or `package-lock.json` change of any kind** (R-B7); `npm audit fix` was not run. `governance-source/` was **not read** (A-055). The frozen demo at `SDS Project Sprint 2` was not touched. Five diagnostic renders — the reference **1440 × 1351** review surface in `draft_ready`, in `trainer_approved` (the state the frame draws), in the **returned** correction state, plus the `/edit` wording and correction surfaces — were written **outside Git** to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F-09\`. Synthetic fixture data only.
- **Unresolved blockers:** none. The four dependencies above are recorded for the operator, not blocking. **OD-3 stays open by instruction.**
- **Contract deviations requested:** **one adjudication is requested, not assumed — D2, the panel headings.** The frame's *Overview / Strengths / Areas for Development / Remarks* were **not** adopted, because they are not a rename of the governed four parent-facing panels and adopting them would redefine what each stored field means to a parent. If the operator rules the frame's headings authoritative, that is a **DTO-and-spec-level** decision (spec §8 / `CLAUDE.md` §6), not a presentation tweak, and needs its own authorization. **Recorded but deliberately NOT fixed, because they are outside this screen's owned paths:** `portal-shell.tsx`'s nav/subtitle contrast (2.225:1 – 3.079:1); `brand-mark.tsx:86`'s hardcoded `href="/trainer"` in the Parent and Management shells; the absent skip-to-content bypass link; `trainer-dashboard.tsx:72`'s undefined `bg-warning-800` token; the unlayered `*`, `body`, `h1`–`h4`, `.card`, `.panel` and `.form-field` rules in `app/globals.css` (worked around here, as at F-05 and F-07, by carrying heading colour on an inner `span`); and the `brand-700` white-on-pink primary action at 4.517:1.
- **Next action:** Operator review of F9, including the **D2 panel-heading adjudication**. **F8 remains BLOCKED on its four non-vocabulary blockers.** Screen `19` / F12 and beyond were **not** started — one screen, one checkpoint, one stop. The canonical index route for screen `10`, and OD-3, remain **unexecuted** and are F16 / route-migration work.

### 2026-08-06 — FRONTEND RECONSTRUCTION F12 (19 Management Student Report)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F12` — screen `19` Management Student Report, node `648:330`, pinned route `/management/reports/[reportId]/review` (+ the `/edit` wording-only sub-surface), physical-test flow order 9. Executed as operator checkpoint **F-12**. This is the **second human decision of the two-stage workflow** — the only surface in the product from which a report becomes parent-visible.
- **Starting commit:** `879549692bb592fc14f2de538975fd4b7a84525d` (F9)
- **Ending commit:** The commit created by `feat(frontend): reconstruct management student report`
- **Status:** Ready for review — **screen `19` proposed visually accepted for its shell, spacing, typography and the allowed parent-facing wording panels ONLY**, because operator ruling **R-B5** removes the majority of what the frozen frame draws. The `/edit` sub-surface is **not** proposed accepted; no frame covers it.
- **Reference integrity:** `19-management-student-report/reference.png` SHA-256 `394d8475…4e6f0` verified **before** any work began and **unchanged after** — 1440 × 1330, 281,963 bytes, byte-identical to the tracker checksum table and to `screen.md` §3.
- **OPERATOR RULING R-B5 EXECUTED — GOVERNANCE OVERRODE THE FROZEN SCREENSHOT, AND THIS IS THE CHECKPOINT'S PRIMARY DELIVERABLE.** Reference 19 contains multiple prohibited elements. **Six were OMITTED, each because a ratified rule forbids it on a Management surface, and each is recorded rather than silently resolved (A-045):** **P1** the **"Report for: Parent / Management" audience toggle** — there is no `kind` enum and no `audience` column on a version; canonicity comes from the aggregate's pointer and audience from **authorization**, never an attribute on the row (A-038), so the toggle would assert a per-audience artefact this system deliberately does not have. **P2** the **"Performance Summary" raw per-dimension rating grid** — Management never reads raw per-dimension ratings (A-038; `CLAUDE.md` §6; `GLOBAL_UI_RULES` §4); this is the same class of leak already caught once on the Parent surface. **P3** **"Overall Grade: Mastering"** — no governed overall or roll-up competency grade exists, and computing one would manufacture a **derived assessment fact this frontend invented** (reserved to the governed assessment, A-034/A-035) while disclosing the rating substance P2 forbids. **P4** **Trainer observations, Trainer notes and every assessment-editing control** — Management never modifies a rating, observation, attendance record, evidence item, Trainer note or any underlying assessment fact (A-034); such an issue is **ALWAYS a return, NEVER a Management edit**. **P5** **"Class Video Evidence"** and attendance substance — outside Management's read entirely (A-038); note that screen 10 keeps this region **inert with a reason** for the Trainer, whereas here it is **OMITTED OUTRIGHT**, because inert-with-reason is the treatment for an **unbacked** affordance, not for a **prohibited** one. **P6** **"Save as draft"** — no governed Management draft state exists among the eight authorized `report_status` values (A-036), and the only Management write to content is the bounded wording save on `/edit`.
- **THE REQUIRED NEGATIVE ASSERTION EXISTS AND IS STRUCTURAL, NOT A PROSE MATCH.** `assertManagementSurfaceClean()` runs against **five Management surface states** in the production DOM — the final review at rest, the final review after reapproval, the wording editor, and both screen-29 queue projections. It asserts: **zero** raw competency-rating tokens under **both** vocabularies (the ratified four plus the three superseded, retained as a regression guard); **zero** elements carrying `data-rating`, `data-rating-level` or `data-evidence-state`; **zero** `video`/`iframe`/`audio`/`source`/`track` elements; **zero** leaf elements whose entire text is one of the **nine governed dimension display names** (the grid-cell form P2 would take); and the absence of the prohibited labels themselves — `Performance Summary`, `Overall Grade`, `Class Video Evidence`, `Report for`, `Save as draft`, `Trainer note`, `Coach Notes`, `Quality Checklist`, `Attendance`, `content hash`, `AI draft`, `AI history`, `audit`, `revision`. **The rating check is deliberately NOT a bare-word regex** — A-052 prohibits one, and the four parent-facing panels are precisely the prose Management is supposed to read, so an isolated-value check is used instead. Screen 19 is additionally asserted to render **exactly four** `[data-report-panel]` elements, and the editor **exactly four** form controls of any kind — the wording-only boundary proved by counting, not by inspection.
- **ONE ON-SCREEN SENTENCE WAS WEAKENED ON PURPOSE, AND THE REASON IS RECORDED.** The rail first explained the omission by naming it ("A single overall grade is deliberately not shown…"). That tripped the new assertion, which forbids the phrase `Overall Grade` anywhere on a Management surface. **The assertion was kept and the prose was reworded** — an explanatory sentence is not worth weakening the check that proves the leak stayed closed. The full reason now lives in the component header, in `implementation-notes.md` and here, which is where R-B5 requires it.
- **THE CONTENT HASH IS NEVER RENDERED AND NEVER REACHES THIS SURFACE (A-038).** The content hash covers the four panels **plus the nine ratings**; a reader holding the panels and that hash recovers the exact per-dimension grid in **4^9 = 262,144** trials — the leak this project already fixed once. This screen holds only `wordingHash`, the **separate, domain-separated** proof over the four parent-facing panels alone, and even that is carried to the server as a concurrency proof and is **not displayed**. The suite asserts `content hash` never appears.
- **THE GOVERNED MANAGEMENT POWERS, BUILT EXACTLY AND NO WIDER (A-034, A-038).** **(1) Wording only** — the four parent-facing panels are the entire editable set, on `/edit`; every accepted change creates a **new immutable version** (A-037) and requires **no** Trainer reapproval. **(2) Return** — a rating, observation or derived-assessment-fact concern with an **exact reason**, always a return and never an edit; the rating scope additionally names the affected dimension, which is part of the governed return input, not a rating read. **(3) Approve & Submit** — the one user action performing two transitions in one transaction, `trainer_approved -> approved -> submitted`, carrying `expectedLockVersion`, `expectedVersionId` and `expectedWordingHash`. Its confirmation now reads **exactly** the ratified sentence — *"Approve and submit [Student]'s report?"* / *"This will publish the final report, notify the linked parent, and update the student record."* — **no dedicated mockup exists and none was invented beyond that description**, and the browser suite pins both strings. The rail states plainly that **the server independently re-verifies authorization, the Trainer approval behind this exact version and the wording proof**, so nothing implies the UI is the gate.
- **Management still reads exactly two things.** `getManagementReview` returns `unavailable` for every status other than `trainer_approved`, and that refusal is server-side — this screen's rendering is not the boundary. No pre-trainer-approval draft, no AI generation history, no audit row.
- **Learner identity comes from a governed projection or not at all.** `ManagementReviewDto` carries **no** student name and **no** session date, yet the ratified confirmation names the learner. Rather than invent a field, the name and date are narrowed out of the **same** `listManagementPendingReviews()` projection screen 29 already reads; a miss omits the row and falls back to *"this learner"* rather than fabricating one. This is the F-09 `listTrainerSessions()` pattern, unchanged. **No DTO, port method, permission, route or fixture was added or changed** — `lib/frontend/contracts/`, `lib/frontend/fixtures/` and `lib/frontend/physical-test-port.ts` are **byte-unchanged**, and the **route census is unchanged at 17 application routes** plus `/_not-found`.
- **Six further frame-versus-governance deviations recorded, not resolved locally.** **D1 PANEL HEADINGS** — the frame's *Overview / Strengths / Areas to Grow / Remarks* are **not** a rename of the governed four (spec §8): "Overview" and "Remarks" have no governed counterpart, and adopting them would silently redefine what each **stored** field means to a parent. The frame's four-section composition, iconography, bullet treatment and order are reproduced; the **governed labels are kept**. **This is the identical adjudication F-09 raised as its D2, and it is re-raised here rather than assumed settled.** **D2** the subtitle *"Public Speaking · Term 1, 2035 · Management copy"* — *"Management copy"* is the same per-audience claim as P1 and is replaced by the governed lifecycle state; class-module, lesson and term are carried by **no** governed Management projection and are omitted rather than fabricated. **D3** the frame's four section glyphs (alert circle, star, arrow, heart) are **not in the approved asset set**; approved icons are reused rather than re-drawing one ad hoc (`GLOBAL_UI_RULES` §8), matching the mapping screen 10 already uses. **D4** the frame's per-section **pencil** is honoured as a link to the governed wording-only editor — it edits nothing in place. **D5** the left rail is `components/layout/portal-shell.tsx`, **outside the owned paths** — the same adjudication F-05, F-09 and F-11 recorded. **D6** the frame's approve-panel copy *"This makes the report no longer editable, and sends notification to the parents"* is replaced by the ratified description.
- **Three dependencies recorded, none invented:** (1) a **lesson number / lesson title** field, (2) a **term** field, (3) **class-module identity** on the Management review projection. All three appear in the frame, none exists on any governed Management DTO, and none was fabricated. **A governed overall grade and a Management-readable rating grid are NOT recorded as dependencies** — they are **prohibited**, not missing, and recording them as gaps would misfile a governance boundary as a backlog item.
- **Vocabulary:** the ratified A-049 vocabulary was already live on this branch (F-06, `5dcbeeb`, verified before any code was written). **This screen renders no competency-rating label at all**, in either vocabulary — that is the point of P2 — and the browser suite proves it structurally. **No anchor was reworded, no enum label renamed, and no global keyword replacement was performed** over `advanced`, `secure`, `emerging`, `beginning`, `mastering` or `mastered` (A-054). **Class Grade is a different, unchanged vocabulary** and is not rendered on this surface at all.
- **The `/edit` wording-only sub-surface was restyled, not reconstructed, and is deliberately NOT proposed accepted.** `reference.png` covers node `648:330` — the review surface only. It is one of the **eight families the Figma matrix §0.1 records as `Blocked — new design required`**, so rather than fabricate a mockup (`GLOBAL_UI_RULES` §8 stop-and-ask) it was converged onto the **shared F1 foundation its sibling now uses** — tokens, card surfaces, field treatment, button primitives — with **no behaviour, field, label, validation rule or governed call changed**. It also retired this file's last uses of the legacy `navy-*` aliases and the pre-foundation type scale.
- **Files changed:** `features/management/management-report-review.tsx`; `features/management/management-wording-editor.tsx`; `tests/frontend/three-role-browser-smoke.mjs` (**assertions only** — additive and **strengthened**: the two waypoint strings that named removed copy were repointed, and the previous flat `assertTextAbsent` list was **replaced by a strictly stronger structural check**, never weakened); this frontend workstream log. **No route file, no shared component, no DTO, no port, no fixture, no `package.json` and no `package-lock.json` was touched.**
- **Tests and validation:** `19-management-student-report/reference.png` SHA-256 verified **before and after — unchanged** (`394d8475498602aee27675d8437ee9395316c45da986b5a8f4db46a9ef94e6f0`); `node_modules/.bin/tsc --noEmit` — exit 0; `node_modules/.bin/eslint .` — exit 0; `npm run build` — exit 0, **route census unchanged at 17 application routes** plus `/_not-found`, none added, removed or renamed; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0, now **13 check groups** including the three new screen-19 groups; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0; compiled `fixture-contract.assertions.ts`, `fixture-lifecycle.assertions.ts`, `design-foundation.assertions.ts` and `auth-reference-fidelity.assertions.ts` — exit 0 each; `git diff --check` — exit 0. **All three browser suites ran against a PRODUCTION build (`next build` + `next start` on port 3412), never a cold dev server**, and the server was restarted onto the final build before the final run. **Zero uncaught browser-console/runtime errors** in every run. **No dependency, no test runner, no `package.json` or `package-lock.json` change of any kind** (R-B7); `npm audit fix` was not run. `governance-source/` was **not read** (A-055). The frozen demo at `SDS Project Sprint 2` was not touched. Four diagnostic renders at the reference **1440 x 1330** viewport — the review surface at rest, the ratified Approve & Submit confirmation, the bounded return dialog with the rating scope selected, and the wording-only editor — were written **outside Git** to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F-12\`. Synthetic fixture data only.
- **Unresolved blockers:** none. The three dependencies above are recorded for the operator, not blocking.
- **Contract deviations requested:** **one adjudication is re-raised, not assumed — D1, the panel headings**, identical to F-09's D2 and still unanswered. If the operator rules the frame's headings authoritative, that is a **DTO-and-spec-level** decision (spec §8 / `CLAUDE.md` §6), not a presentation tweak. **Recorded but deliberately NOT fixed, because they are outside this screen's owned paths:** `portal-shell.tsx`'s nav/subtitle contrast (2.225:1 - 3.079:1); `brand-mark.tsx:86`'s hardcoded `href="/trainer"` in the Management shell — **live on this screen**, where the brand mark links a Management user into the Trainer portal; the absent skip-to-content bypass link; `trainer-dashboard.tsx:72`'s undefined `bg-warning-800` token; the unlayered `*`, `body`, `h1`-`h4`, `.card`, `.panel` and `.form-field` rules in `app/globals.css` (worked around here, as at F-05, F-07 and F-09, by carrying heading colour on an inner `span`); and the `brand-700` white-on-pink primary action at 4.517:1, which is this screen's **Approve & Submit** control.
- **Next action:** Operator review of F12, including the **D1 panel-heading adjudication** and confirmation that the **R-B5 omission set is complete and correctly classified**. **F8 remains BLOCKED on its four non-vocabulary blockers** — note that its rating-grid and overall-grade blockers are the **same two prohibited elements** ruled out here, on a Trainer surface where they are permitted, so R-B5 does **not** unblock F8. Screen `33` / F15 and beyond were **not** started — one screen, one checkpoint, one stop. The canonical `[studentId]` route for screen `19` remains **unexecuted** and is F16 / route-migration work.

### 2026-08-06 — FRONTEND RECONSTRUCTION F15 (33 Parent Class Report)

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F15` — screen `33` Parent Class Report, node `627:9`, implemented route `/parent/students/[studentId]/sessions/[sessionId]/report`, physical-test flow order 12. Executed as operator checkpoint **F-15**.
- **Starting commit:** `9feedb08338ee64b67fba20a5a39bc904ad96e5d` (F12)
- **Ending commit:** The commit created by `feat(frontend): reconstruct parent class report`
- **Status:** Ready for review — **screen `33` proposed visually accepted**
- **Reference integrity:** `33-parent-class-report/reference.png` SHA-256 `2aaeb446…dea67` verified **before** any work began and **unchanged after** — 1440 × 1340, 293,726 bytes, byte-identical to the checkpoint brief and to `screen.md` §3.
- **Scope completed:** The report was rebuilt from the frozen frame's structure: page title **"Class Report"** with a received-date meta line, then **one white report card** carrying a bordered header row (tinted document tile · "Class Report" · submitted-date meta) above a **vertical stack of narrative sections**, each composed as **soft tinted icon tile · bold section heading · one bullet of prose** — the frame's own section rhythm, and its star / rising-arrow / target / heart section marks in the shared stroked line style. The previous two-column `lg:grid-cols-2` card grid and its uppercase eyebrow labels were replaced. **F-15 was explicitly not a "remove the leak" job — the previous implementation was checked first and was already compliant, and this checkpoint's obligation was to raise the fidelity without INTRODUCING a leak.** Loading, failed, unavailable and denied states are all preserved and built; the unavailable and denied copies remain byte-identical and non-disclosing. **No route, DTO, port method, permission or fixture was added or changed.**
- **GOVERNANCE OVERRODE THE FROZEN SCREENSHOT — this is the primary deliverable of F-15 (operator ruling R-B6), and screen `33` carries the largest frame-versus-governance gap in the pack.** **FOUR things the frame draws are DELIBERATELY OMITTED, each recorded and none resolved locally.** **D1 — the "Performance Summary" per-dimension grid.** The frame draws a four-cell grid of raw dimension:rating pairs (SPEECH · MASTERING, TONALITY · MASTERED, EYE CONTACT · BEGINNING, AUDIENCE AWARENESS · DEVELOPING) on a Parent surface. This is the **already-caught leak `CLAUDE.md` §6 names by name** — "No per-dimension rating grid on the Parent Feedback Report, in any form or wording — this is a caught leak, fix it" — and it is **not recreated in any form, including softened wording**. The screen's "simplified performance summary" requirement is satisfied by the four prose panels and by nothing else. It is also the exact conflict **F1 recorded and deferred to this checkpoint** (log line 148), now discharged: the ratified rule governs and the frame does not. **D2 — "Overall Grade: Mastering".** An aggregate competency grade is a rating value; no Parent surface renders a rating token in either vocabulary (A-021; A-048; GLOBAL_UI_RULES §5). Omitted. **D3 — the prose rating attributions.** The frame's body copy reads "Assessed as Mastered in eye contact … and Mastering in body language", "currently assessed as Developing", and "to progress these skills to the Mastering band" — explicit rating attribution and taxonomy disclosure, precisely the form A-052 authorises detecting. **The frame's copy is not ported**; the panels render the governed submitted narrative the port returns and nothing is written over it. **D4 — the "Watch Together" evidence video.** Parent evidence access is gated on **all** of: the report having reached `submitted`, the `evidence_media` consent scope, and a short-TTL server-minted signed URL scoped to the requesting parent's `parent_student_links` row (A-001; `CLAUDE.md` §6) — and beyond that, **evidence scope and the uploader are UNRESOLVED (A-014)**. No such governed path exists, so the region is **omitted rather than faked or rendered inert**, and **no media element renders on the surface at all**. **The stop condition was evaluated explicitly and did not fire: none of the four was implemented.**
- **Required negative assertion — load-bearing privacy evidence, and it is proven falsifiable rather than merely green.** `tests/frontend/three-role-browser-smoke.mjs` now carries `assertParentSurfaceClean(label)`, applied to **every** Parent surface — the report list before and after submission, the Parent home surface and the canonical detail. It proves, in the **rendered production DOM**, that no rating token, no per-dimension grid, no overall grade, no evidence media, no correction history, no content hash and no version metadata renders; it also pins the detail to **exactly the four governed panel headings in order**. **The check is deliberately NOT a bare-word prose regex — A-052 prohibits one**, because "at the beginning of the session" and "has mastered maintaining eye contact" are legal parent-facing prose the canonical narrative may contain. What is detected is the form A-052 authorises: a **leaf element whose entire text is one raw label or dimension name**, plus rating-, evidence-, hash- and version-bearing data attributes, plus any `video`/`iframe`/`audio`/`source`/`track`/`embed`/`object` element. **Falsifiability was proven, not assumed:** an "Overall Grade" row, a `Mastering` chip, an `Eye Contact` dimension cell and a `<video>` were deliberately injected and rebuilt, and the suite failed with `Parent canonical DOM leaked forbidden text: Mastering` and then, with the vocabulary token removed, `rendered a prohibited internal element: dimension-cell:span="eye contact", media:video`. The injection was reverted and the final build re-verified green.
- **Two further frame-versus-governance deviations — recorded, not silently resolved:** **D5** the frame's **"Report Details" sidebar** is not reconstructed: its "Overall Grade" row is prohibited outright (D2), and its **Name / Class / Lesson / Term** rows have **no governed source** — `CanonicalReportDto` carries exactly `panels` and `submittedAt`. Those fields are **omitted and recorded as a dependency rather than fabricated or back-derived from a route parameter** (GLOBAL_UI_RULES §10). **D6** the page subtitle is rendered locally rather than through `PageHeading`'s `description` prop, which resolves to `text-ink-muted` `#8a93a8` and fails the 4.5:1 AA floor — the shared primitive was left untouched and the failure recorded, the same trade F11 and F14 made.
- **Two dependencies recorded, none invented:** (1) **learner name, class grade, class module, lesson and term** are not carried by the governed `CanonicalReportDto`, so the frame's identity header and sidebar rows are omitted — nothing was faked client-side and no DTO field was added; (2) **a governed parent evidence-media path does not exist** — the gating is ratified but scope and uploader are UNRESOLVED (A-014), so the "Watch Together" region is omitted rather than stubbed.
- **Functional behaviour preserved:** the pinned route, `port.getCanonicalReport(sessionId, studentId)`, the `app_parent_reaches_student` reachability check, submitted-only version resolution, and every resolved state. The surface remains **view-only with no edit affordance of any kind**, and this component adds **no client-side condition that grants access**.
- **Not rating-bearing:** screen `33` surfaces no competency-rating vocabulary (`screen.md` §8), so F15 carries **no F6 dependency**. No Class Grade artefact was touched and **no keyword replacement** over `advanced`/`secure`/`emerging`/`beginning`/`mastering`/`mastered` was performed anywhere (A-054).
- **Accessibility:** the page `h1` is followed by the card `h2` and the four panel `h3`s, so the outline does not skip; the report card is a `section` with an accessible name; every icon tile and prose bullet is `aria-hidden` decoration carrying no meaning that colour or shape alone conveys; body prose is `text-ink` rather than the sub-AA `text-ink-muted`; the surface is static and view-only, so it introduces no new interactive target.
- **Files changed:** `features/parent/parent-canonical-report.tsx`; `tests/frontend/three-role-browser-smoke.mjs` (the new `assertParentSurfaceClean`, its four call sites, the four-panel-heading pin and the check label); this frontend workstream log.
- **Tests and validation:** `33-parent-class-report/reference.png` SHA-256 verified **before and after — unchanged**; `npx tsc --noEmit` — exit 0; `npx eslint .` — exit 0; `npm run build` — exit 0, **route census unchanged at 17 application routes** plus `/_not-found` — none added, removed or renamed; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0; `git diff --check` — exit 0. **Every browser smoke ran against a production build** (`next build` + `next start` on ports 3115/3118), never a cold dev server, with **zero uncaught browser-console/runtime errors**. Diagnostic renders were written **outside Git** to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F-15\`. Synthetic fixture data only.
- **Unresolved blockers:** none. The two dependencies above are recorded for the operator, not blocking.
- **Contract deviations requested:** none. **Recorded but deliberately NOT fixed, because they are outside this screen's owned paths:** `portal-shell.tsx`'s nav/subtitle contrast (2.225:1 – 3.079:1, SC 1.4.3); `brand-mark.tsx:86`'s hardcoded `href="/trainer"` in the Parent shell, which this screen's left rail inherits; the absent skip-to-content bypass link (SC 2.4.1); `trainer-dashboard.tsx:72`'s undefined `bg-warning-800` token; the unlayered `*`, `body`, `h1`–`h4`, `.card`, `.panel` and `.form-field` rules in `app/globals.css`; and the `brand-700` white-on-pink primary action at 4.517:1.
- **Next action:** Operator review of F15, including confirmation that the **R-B6 omission set for screen `33` is complete and correctly classified** — D1 the per-dimension grid, D2 the overall grade, D3 the prose rating attributions, D4 the evidence video. **This closes the final parent screen of the twelve-screen physical-test flow.** **F8 remains BLOCKED on its four non-vocabulary blockers.** **F4 (05 Trainer Schedule) remains blocked** on the operator route decision (inventory §7.3, U-A5-1). The **canonical `/parent/reports/[reportId]` route with server-side pair resolution remains UNEXECUTED** and is F16 / route-migration work. Screen `33` was the only screen touched — one screen, one checkpoint, one stop.

---

### 2026-08-06 — FRONTEND RECONSTRUCTION F8 (08 Trainer AI Report Generation) — RETRY, now BUILT

- **Timestamp (Asia/Singapore):** 2026-08-06
- **Round / checkpoint ID:** `FRONTEND RECONSTRUCTION F8` — screen `08` Trainer AI Report Generation, node `784:340`, implemented route `/trainer/reports/[reportId]/generate`, physical-test flow order 5. Executed as operator checkpoint **F-08 (retry)**. The earlier F-08 agent returned **BLOCKED** and was **correct to do so**; its finding was accepted, the withdrawn premise ("this component renders no rating value") described the CURRENT COMPONENT rather than the FROZEN FRAME, and **one** blocker — the ratified vocabulary not being live — was **discharged by F6 (commit `5dcbeeb`, verified)**. The other four blockers **stand and were not resolved here**: they were handled by **governance-wins-and-record**, exactly as F-14 and F-11 did.
- **Starting commit:** `b80b29566525a3f8e106f67c5846ba0365f90541` (F15)
- **Ending commit:** The commit created by `feat(frontend): reconstruct trainer AI report generation`
- **Status:** Ready for review — **screen `08` proposed visually accepted**
- **Reference integrity:** `08-trainer-ai-report-generation/reference.png` SHA-256 `3160524f…e20c4` verified **before** any work began and **unchanged after** — 1650 × 1180, 172,209 bytes, byte-identical to the checkpoint brief and to `screen.md` §3.
- **Scope completed:** The screen was rebuilt from the frozen frame's architecture as the **governed per-session AI draft-generation surface**: the **"AI Report Generation"** page title with a `Schedule / <learner>` breadcrumb, the pink **"Back to Student Roster"** pill, the governed lifecycle pill; a **generated-draft card** with a square learner avatar, a pink `AI Draft — <learner>` title, a class/date/working-version subtitle and the frame's **four icon-led, bullet-led, per-panel-edit content sections**; the **"Class Video Evidence"** region; and the **right rail** carrying **Report Details**, **Performance Summary** and the frame's dark action panel. **The governed generation states the frame draws nowhere are all built** (`screen.md` §6, spec §15): context loading, **drafting** with every forward action rendered disabled, **grounding failure with the assessment preserved and one bounded retry**, the **nine-rating validation refusal** (distinct from the grounding rejection, and deliberately offering **no** retry because retrying cannot satisfy the precondition), **empty** (generation succeeded but the governed projection returned nothing readable — nothing is reconstructed from the generation response), and non-disclosing **unavailable**. **No route, DTO, port method, permission or fixture was added or changed** — `lib/frontend/contracts/`, `lib/frontend/fixtures/` and `lib/frontend/physical-test-port.ts` are **byte-unchanged**, and the **route census is unchanged at 17 application routes** plus `/_not-found`.
- **GOVERNANCE OVERRODE THE FROZEN SCREENSHOT — this is the primary deliverable of F8, and screen `08` carries the second-largest frame-versus-governance gap in the pack after screen `33`.** **D1 — THE FRAME IS A TERM REPORT.** It is headed "Term Report — Alicia Gomez · Public Speaking · Term 1, 2035 · Parent copy". **End-of-term report GENERATION is expressly out of MVP scope** (`CLAUDE.md` §5, §8; v3 §28): the End-of-Term Performance Report is a **separate instrument** on a different 7-criteria / `Excellent`–`Good`–`Needs Improvement` scale whose **evidence is captured now but whose generator is not built**. **Term-report generation was NOT built.** The screen is reconstructed as the per-session draft surface it actually is, and **"Parent copy" is additionally a false lifecycle claim** on a Trainer working version — only management's Approve & Submit makes a version canonical and parent-visible (A-033). Both are omitted and the conflict is recorded. **D3 — CLASS VIDEO EVIDENCE.** The frame draws a working **drag-and-drop uploader** with an "MP4, MOV · up to 500MB each" policy. Evidence scope **and** the uploading role are **UNRESOLVED (A-014)**, the evidence schema is **excluded from the Step 7E boundary**, `PhysicalTestPort` exposes **no** upload or evidence-read path, and `Evidence Pending` is deliberately not a stored status (A-036). The region is **KEPT and rendered INERT** with a visible, programmatically associated reason — the F-04 D1 / F-11 "Send Reminder to Trainer" treatment — **no uploader was invented**, and the frame's format and size limits are **omitted rather than fabricated**, because they would be an unratified policy. **D4/D5 — REPORT DETAILS.** The frame shows **Lesson**, **Term**, a **Class** label and **"Overall Grade: Mastering"**. `DraftGenerationContextDto` carries exactly `reportId`, `studentDisplayName`, `observationLockVersion` and `status`; only what the governed projections supply is rendered (Name, Class, Session date, Version, Status). Lesson and Term are **omitted and recorded as dependencies**; **Overall Grade is omitted outright** because a single headline rating would be a **derived assessment fact this frontend computed**, exactly the class of claim A-034/A-035 reserve to the governed assessment — and the 9→7 roll-up and 4→3 scale map remain **provisional** (`CLAUDE.md` §5) and belong to the unbuilt term generator anyway. **The omission is stated on screen, not left as a silent gap.** **D7 — "CONFIRM & SUBMIT" AS A TRAINER PRIMARY CONTROL WITH NO QUALITY CHECKLIST.** This contradicts the ratified two-stage workflow (A-033, A-036) on three counts: the Trainer action is **APPROVE**, not submit; **the Trainer does not publish**; and the approve gate is the **three-item version-scoped Quality Checklist**, which the frame draws nowhere. **The governed action was implemented instead of the frame's control** — the dark panel hands off to Review & Approve and states the gate and the two-stage sequence plainly ("You do not publish, and no parent is notified at this step"). **"Save as draft" is OMITTED**: the validated draft is already stored as an immutable version by the governed store path inside generation (RPC-4), so a second, differently governed save affordance would be an invented mutation. **The stop condition was evaluated explicitly and did not fire — a governed screen genuinely remains, so it was built rather than re-blocked.**
- **Four further frame-versus-governance deviations — recorded, not silently resolved:** **D2** the frame's panel headings "Overview / Strengths / Areas to Grow / Remarks" are **not** a rename of the governed four (Today's Strength / Next Focus / Practice Suggestion / Session Takeaway, spec §8) — "Overview" and "Remarks" have no governed counterpart and a mapping would silently redefine what each stored field means to a parent; the frame's composition, iconography and **per-panel edit affordance** are reproduced and the governed labels kept (identical to F9 D2), with the pencil routed to the **one** governed edit transaction over all four panels rather than editing in place, because a trainer edit resets the checklist and creates a new immutable version (A-021, A-037). **D6** "PERFORMANCE SUMMARY" draws **four** tiles; **all nine dimensions are mandatory (A-017)** and no governed rule selects a subset, so the frame's tile composition is reproduced across **all nine** governed snapshots. **D8** the frame's second "REVIEW & APPROVE" rail (four count chips plus a per-student lifecycle list) is the roster/queue surface, outside owned paths; the relationship is carried by the Back to Student Roster control the frame also draws. **D9** the left rail is `portal-shell.tsx`, outside owned paths.
- **Ratings on this surface are LEGITIMATE and vocabulary-correct.** Screen `08` is a **Trainer** surface, so the nine per-dimension snapshots are the Trainer's own source check against the assessment substance the draft was grounded on. They render the ratified **Beginning / Developing / Mastering / Mastered** labels from the single declared `RATING_DISPLAY_LABELS` map (A-049), never a hand-written string. **Class Grade** in the subtitle and rail is the **different, unchanged** vocabulary (A-054) and marks itself `data-vocabulary="class-grade"` so a token guard classifies by **actual context**. **No keyword replacement over `advanced`/`secure`/`emerging`/`beginning`/`mastering`/`mastered` was performed anywhere.** The management and parent boundaries are unchanged and are proved separately by the three-role smoke.
- **AI governance held, and asserted rather than asserted-about.** Grounding runs **before** any draft reaches this screen — the port's `requestDraft` is the whole RPC-3 → grounding → RPC-4/RPC-5 path, a rejection returns `generation_failure`, and **the rejected wording is never rendered**. Panels and ratings are read back through `getTrainerWorkingReport` **only after** a stored, validated draft exists, so unvalidated model output is never displayed. The **content hash is never rendered** (A-038 — it covers the four panels **plus** the nine ratings). **AI never approves, submits or publishes**: the only forward affordances are human review and human edit. The failure state is a **designed recovery with its own copy and a bounded retry**, not a generic error toast (persona §3.8, spec §15), and the browser smoke now proves in the production DOM that **zero panels and zero rating snapshots render while the draft stands rejected**.
- **Functional behaviour preserved:** the pinned route, the fixture-port / `AiDraftProvider` boundary, deterministic grounding, explicit provider selection with no silent choice, the governed draft store/cancel path, the single-flight generation guard, and every previously-passing smoke step (`Draft rejected safely`, `Retry once`, `Grounded draft ready`, `Review four-panel report`).
- **One smoke-harness correction, made honestly rather than by weakening copy:** the screen-10 hand-off previously waited on the text `Quality Checklist`. Screen `08` now **names the governed gate** in its hand-off copy, so a text-only wait would settle on the generation surface and every following screen-10 assertion would silently run against the wrong page. The wait is now keyed on the **three real checklist inputs** plus the heading. This was found by the suite failing, not reasoned about.
- **Accessibility:** the page `h1` is followed by the card `h2`s and the panel `h3`s, so the outline does not skip; the drafting state is `aria-busy` with a polite live region; the inert evidence control is a real `disabled` button with `aria-describedby` pointing at its stated reason; each per-panel edit link carries a distinct accessible name; colour is never the only carrier — every rating tile states its level in text — and **all four rating states were re-measured in the rendered production DOM at 5.134:1 – 5.733:1**, clearing the 4.5:1 SC 1.4.3 floor.
- **Files changed:** `features/trainer/trainer-draft-generation.tsx`; `tests/frontend/trainer-browser-smoke.mjs` (the screen-08 assertion block, the failure-state containment assertions, the screen-08 screenshot, the corrected screen-10 wait, and the check label); this frontend workstream log.
- **Tests and validation:** `08-trainer-ai-report-generation/reference.png` SHA-256 verified **before and after — unchanged**; `npx tsc --noEmit` — exit 0; `npx eslint .` — exit 0; `npm run build` — exit 0, **route census unchanged at 17 application routes** plus `/_not-found` — none added, removed or renamed; `node tests/frontend/trainer-browser-smoke.mjs` — exit 0; `node tests/frontend/three-role-browser-smoke.mjs` — exit 0; `node tests/frontend/authentication-browser-smoke.mjs` — exit 0; `git diff --check` — exit 0. **Every browser smoke ran against a production build** (`next build` + `next start` on port 3308), never a cold dev server, with **zero uncaught browser-console/runtime errors**. Diagnostic renders were written **outside Git** to `UI_REFERENCE_FINAL_MVP\_checkpoint-evidence\F-08\`. Synthetic fixture data only.
- **Unresolved blockers:** none blocking this checkpoint. **Four dependencies recorded for the operator, none invented:** (1) **term-report generation** is out of MVP scope and the frame's term framing is therefore not implemented — if the frame is meant to be authoritative, that is an amendment, not a frontend fix; (2) **lesson number and term** are carried by no governed Trainer projection; (3) **no governed evidence upload or read path exists**, and scope plus uploader remain UNRESOLVED (A-014); (4) the **assessment surface cannot be reached from a refused generation** because `DraftGenerationContextDto` carries no `sessionId`/`studentId`, so the validation state routes to the schedule and says so rather than guessing a route.
- **Contract deviations requested:** none. **Recorded but deliberately NOT fixed, because they are outside this screen's owned paths:** `portal-shell.tsx`'s nav/subtitle contrast (2.225:1 – 3.079:1, SC 1.4.3); `brand-mark.tsx:86`'s hardcoded `href="/trainer"`; the absent skip-to-content bypass link (SC 2.4.1); `trainer-dashboard.tsx:72`'s undefined `bg-warning-800` token; the unlayered `*`, `body`, `h1`–`h4`, `.card`, `.panel` and `.form-field` rules in `app/globals.css` (worked around here by colouring an inner `span`, as F9 did); and the `brand-700` white-on-pink primary action at 4.517:1. **The frame's per-panel pencil glyph is rendered with the approved `document` icon** — the shared set carries no pencil, and no icon was re-drawn ad hoc (`GLOBAL_UI_RULES` §8).
- **Next action:** Operator review of F8, including confirmation that the **governance-wins-and-record set for screen `08` is complete and correctly classified** — D1 the term-report framing and "Parent copy", D3 the evidence uploader, D4/D5 Lesson/Term/Overall Grade, D7 "Confirm & Submit" and "Save as draft". **This closes the last screen of the twelve-screen physical-test flow that was still outstanding on governance grounds.** **F4 (05 Trainer Schedule) remains blocked** on the operator route decision (inventory §7.3, U-A5-1). The **canonical `/trainer/schedule/[sessionId]/student-roster/[studentId]/grade-student/ai-report-generation` route with its pinned-path redirect remains UNEXECUTED** and is F16 / route-migration work. Screen `08` was the only screen touched — one screen, one checkpoint, one stop.
